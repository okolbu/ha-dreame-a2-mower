# tools/

Standalone scripts that read or write integration state without
needing a running Home Assistant. Run from the repo root:

```
python3 tools/<script>.py [args]
```

See individual scripts for usage and flags.
