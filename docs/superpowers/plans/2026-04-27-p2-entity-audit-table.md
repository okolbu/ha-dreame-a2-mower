# P2 entity audit table

**Date**: 2026-04-27
**Plan**: docs/superpowers/plans/2026-04-27-pre-launch-p2-entity-audit.md
**Source-of-truth**: docs/research/g2408-protocol.md §2.1

## Disposition rules

| §2.1 status | Classification |
|---|---|
| Confirmed | PRODUCTION / OBSERVABILITY |
| Partial | OBSERVABILITY |
| TBD on g2408 | EXPERIMENTAL (disabled by default) |
| enum-only / none | DELETE |

## Section 1.1 — binary_sensor.py + camera.py + lawn_mower.py + device_tracker.py

> **Row count**: 12 total (5 binary_sensor + 5 camera + 1 lawn_mower + 1 device_tracker).
> The spec estimated ~16; actual descriptor count is lower: binary_sensor.py has 5 descriptors (not 8) and camera.py has 5 distinct entity types (not 7).

| File | Class/key | Computed entity_id | Underlying property/action | §2.1 citation | Classification | Proposed entity_id | Proposed entity_category | Proposed enabled_by_default | Reason / notes |
|---|---|---|---|---|---|---|---|---|---|
| binary_sensor.py | `obstacle_detected` | `binary_sensor.dreame_a2_mower_obstacle_detected` | `property_key=DreameMowerProperty.OBSTACLE_FLAG` → s1.53 bool | s1.53 (confirmed) | PRODUCTION | — | `None` | `True` | Directly actionable: user should see obstacle/person alerts on dashboard. s1p53 is confirmed in §2.1; has auto-clear 30 s guard for latch-without-clear behaviour (binary_sensor.py:27). |
| binary_sensor.py | `mowing_session_active` | `binary_sensor.dreame_a2_mower_mowing_session_active` | `value_fn` → `device.status.started` (from s2.56 task-state) OR `device.has_active_in_progress` (disk-restored flag) | s2.56 (confirmed) | PRODUCTION | — | `None` | `True` | Orthogonal to lawn_mower state: stays True through pause/charge mid-session; the only entity that signals "session is still running" while the mower is physically docked for recharge. Feeds useful automations ("notify when mowing finishes"). |
| binary_sensor.py | `battery_temp_low` | `binary_sensor.dreame_a2_mower_battery_temp_low` | `value_fn` → `device.battery_temp_low` → s1.1 heartbeat byte[6]&0x08 | s1.1 (confirmed, HEARTBEAT byte[6] bit) | PRODUCTION | — | `None` | `True` | User-actionable: mower refuses to charge when asserted; coordinator also fires a persistent notification (coordinator.py:390). Sourced from confirmed heartbeat decode (§4.4). |
| binary_sensor.py | `positioning_failed` | `binary_sensor.dreame_a2_mower_positioning_failed` | `value_fn` → `device.positioning_failed` → s2.2 code 71 | s2.2 / s2.56 (confirmed — s2p2 secondary phase channel; code 71 = positioning failed) | PRODUCTION | — | `None` | `True` | User-actionable: while True every cloud-issued Start/Dock/Return fails until SLAM relocation recovers. Enables automation "alert when mower is lost". s2p2 phase channel is confirmed on g2408 (device.py:354). |
| binary_sensor.py | `rain_protection_active` | `binary_sensor.dreame_a2_mower_rain_protection_active` | `value_fn` → `device.rain_protection_active` → s2.2 code 56 | s2.2 / s2.56 (confirmed — s2p2 secondary phase channel; code 56 = BAD_WEATHER / rain) | PRODUCTION | — | `None` | `True` | User-actionable: mower parked at dock due to rain; useful dashboard indicator and automation trigger. Same confirmed s2p2 channel as positioning_failed, different code. §2.1 §ERROR code 56 = BAD_WEATHER. |
| camera.py | `map` (key="map") | `camera.<device_name>_map` | `DreameMowerMapType.FLOOR_MAP` — renders live map PNG from MAP_DATA (s6.1) + live trail overlay from s1.4 telemetry | s6.1 (confirmed, MAP_DATA), s1.4 (confirmed, MOWING_TELEMETRY) | PRODUCTION | — | N/A | `True` | Primary user-facing map entity; confirmed data sources. No rename: name already set dynamically to "Live Map". |
| camera.py | `map_data` (key="map_data") | `camera.<device_name>_map_data` | `DreameMowerMapType.JSON_MAP_DATA` — serves gzip-JSON dump of parsed map state for dev/custom-card use | s6.1 (confirmed) | OBSERVABILITY | — | `EntityCategory.CONFIG` | `False` | Dev/integration-builder tool: exposes raw parsed map JSON for custom Lovelace cards or debugging. Not a user dashboard item. Already `entity_registry_enabled_default=False` (camera.py:108). |
| camera.py | `saved_map` (key="saved_map", dynamic per map index) | `camera.<device_name>_map_<N>` | Static base-map PNG per stored map; uses same `DreameMowerMapRenderer` on stored `MapData` | s6.1 (confirmed) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | User needs to select and view stored maps; each created dynamically in `async_update_map_cameras` (camera.py:397). CONFIG category appropriate — infrastructure, not live state. |
| camera.py | `wifi_map` (key="wifi_map", dynamic per map index) | `camera.<device_name>_wifi_map_<N>` | WiFi heatmap overlay PNG per stored map; gated by `capability.wifi_map && !low_resolution` | s6.3 (confirmed, WiFi signal on g2408) | OBSERVABILITY | — | `EntityCategory.CONFIG` | `False` | Nice-to-have overlay for finding WiFi dead spots, not a dashboard primary. Already `entity_registry_enabled_default=False` (camera.py:419). On g2408 s6.3 carries WiFi [cloud_connected, rssi_dbm] — the wifi_map itself may not be populated; treat as OBSERVABILITY until confirmed working. |
| camera.py | `DreameMowerLidarTopDownCamera` | `camera.<device_name>_lidar_top_down` (unique_id: `<mac>_lidar_topdown`) | Renders top-down PNG from LiDAR PCD archive (99.20 OSS key, local cache) | 99.20 (confirmed, LiDAR PCD trigger) | OBSERVABILITY | — | N/A | `True` | Protocol-debugging / power-user feature: visualises the LiDAR point cloud. Not a primary mowing-state indicator. Enabled by default because it's the only 3D scan view and users asked for it; but not PRODUCTION because it's a dev/exploration tool. |
| lawn_mower.py | `DreameMower` | `lawn_mower.<device_name>` (unique_id: `<mac>_dreame_a2_mower`) | Wraps s2.1 STATUS enum → `LawnMowerActivity`; s3.1 BATTERY_LEVEL; s3.2 CHARGING_STATUS; actions via device.start/pause/dock | s2.1 (confirmed), s3.1 (confirmed), s3.2 (confirmed) | PRODUCTION | — | `None` | `True` | The mandatory primary entity for any lawn_mower platform integration. All three underlying properties are confirmed in §2.1. State mapping covers the full DreameMowerState enum (lawn_mower.py:114). |
| device_tracker.py | `DreameMowerGpsTracker` | `device_tracker.<device_name>_gps` (unique_id: `<mac>_gps`) | Reads `device.gps_latitude` / `device.gps_longitude` from `_locn` cache populated by `getCFG t:'LOCN'` | LOCN (confirmed, WGS84 {pos:[lon,lat]}) | PRODUCTION | — | `None` | `True` | Anti-theft / location tracking. LOCN is confirmed in §2.1 CFG catalog. Entity goes unavailable when gps_latitude is None (device_tracker.py:81), handling the CFG.ATA[2] anti-theft-disabled case cleanly. |

## Section 1.2 — select.py + number.py + time.py

> **Row count**: 12 total (6 select + 2 number + 4 time).
> select.py has 5 descriptors in the `SELECTS` tuple plus `DreameReplaySessionSelect` (separate class). number.py has 1 descriptor in `NUMBERS` plus `DreameMowerStationBearingNumber` (separate class). time.py has 4 descriptors in `TIMES`.
> Entities gated by `capability.X = False` will never instantiate on g2408 — treated as dead-code DELETE candidates.

| File | Class/key | Computed entity_id | Underlying property/action | §2.1 citation | Classification | Proposed entity_id | Proposed entity_category | Proposed enabled_by_default | Reason / notes |
|---|---|---|---|---|---|---|---|---|---|
| select.py | `CLEANING_MODE` | `select.dreame_a2_mower_cleaning_mode` | `property_key=DreameMowerProperty.CLEANING_MODE` → s4.23 | none (s4p23 not in §2.1 catalog; CLEANING_MODE for g2408 mow-mode is decoded from s6.2[1] = FRAME_INFO; s4p23 is a vacuum-era property unmapped on g2408) | DELETE | — | — | — | s4p23 is the vacuum cleaning-mode register, not used on g2408. Mow mode is read-only from s6.2[1] (FRAME_INFO). No MQTT evidence that s4p23 is writable or even polled on g2408. types.py:689 maps it to s4p23 with no g2408 overlay, but the protocol research shows no observation of this property ever firing on g2408. |
| select.py | `VOICE_ASSISTANT_LANGUAGE` | `select.dreame_a2_mower_voice_assistant_language` | `property_key=DreameMowerProperty.VOICE_ASSISTANT_LANGUAGE` → s7.10; gated by `capability.voice_assistant` | none (s7p10 not in §2.1 catalog; `capability.voice_assistant = False` on g2408 — types.py:1170 — so entity never instantiates) | DELETE | — | — | — | `capability.voice_assistant = False` on g2408 (types.py:1170); `exists_fn` returns False; entity is never added. The select.py:83–84 `exists_fn` gate makes this dead code on g2408. s7p10 has no §2.1 entry. |
| select.py | `CLEANING_ROUTE` | `select.dreame_a2_mower_cleaning_route` | `property_key=DreameMowerAutoSwitchProperty.CLEANING_ROUTE`; transport via s2.51 MULTIPLEXED_CONFIG; gated by `capability.cleaning_route` | s2.51 (confirmed for MULTIPLEXED_CONFIG transport) | DELETE | — | — | — | `capability.cleaning_route = False` on g2408 (types.py:1184); `exists_fn` in select.py:91 blocks instantiation. Even though s2p51 is confirmed, this specific sub-field is not observed on g2408 and the capability gate ensures the entity never registers. |
| select.py | `CLEANGENIUS` | `select.dreame_a2_mower_cleangenius` | `property_key=DreameMowerAutoSwitchProperty.CLEANGENIUS`; transport via s2.51 MULTIPLEXED_CONFIG; gated by `capability.cleangenius` | s2.51 (confirmed transport, but CLEANGENIUS sub-field unobserved on g2408) | DELETE | — | — | — | `capability.cleangenius = False` on g2408 (types.py:1180); `exists_fn` in select.py:99 blocks instantiation. Capability gate makes this dead code on g2408. |
| select.py | `selected_map` | `select.dreame_a2_mower_selected_map` | `value_fn` → `device.status.selected_map.map_name`; gated by `capability.map and capability.multi_floor_map` | s6.1 (confirmed MAP_DATA) | DELETE | — | — | — | `capability.multi_floor_map = False` on g2408 (types.py:1160); `exists_fn` in select.py:113 requires both `capability.map` and `capability.multi_floor_map`; entity never instantiates on single-map g2408. |
| select.py | `DreameReplaySessionSelect` | `select.dreame_a2_mower_replay_session` | `value_fn` → session archive list; mode fed to `live_map.set_mode()`; no MQTT property | none (integration-internal; controls HA-side map replay engine; not a protocol property) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Integration-internal session picker: drives the live-map camera overlay to show "Latest", "Blank", or a pinned historical run. Confirmed working (session archive is the integration's own mechanism, not a device property). User-actionable from the dashboard. Already `entity_category=CONFIG` (select.py:321). |
| number.py | `VOLUME` | `number.dreame_a2_mower_volume` | `property_key=DreameMowerProperty.VOLUME` → s7.1 (write); `post_action=DreameMowerAction.TEST_SOUND` → s7 aiid=2; CFG key `VOL` for read | CFG.VOL (confirmed, §2.1 CFG catalog — "Robot Voice volume, 0..100 %") | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Volume slider confirmed real on g2408 via CFG.VOL in §2.1 catalog (g2408-protocol.md:1129). Write goes to s7p1; TEST_SOUND action at s7 aiid=2 plays a beep to confirm. User-actionable setting. Already `entity_category=CONFIG` (number.py:47). |
| number.py | `DreameMowerStationBearingNumber` | `number.dreame_a2_mower_station_direction` | HA-only: reads/writes `entry.options[CONF_STATION_BEARING]`; no MQTT property | none (HA-only config; feeds coordinate rotation in sensor.py; never flows to device) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | HA-only compass-bearing input that seeds the North/East position sensor rotation (number.py:150 comment). Not a device property but directly user-actionable for correct map orientation; no §2.1 citation needed — this is a display-transform parameter. |
| time.py | `dnd_start` | `time.dreame_a2_mower_dnd_start` | `key="dnd_start"` → `device.status.dnd`; property `DreameMowerProperty.DND_TASK` s5.4; gated by `capability.dnd` | CFG.DND (in §2.1 CFG catalog, but marked "apk-catalogued", no g2408 live observation; `capability.dnd = False` on g2408) | DELETE | — | — | — | `capability.dnd = False` on g2408 (types.py:1166); `exists_fn` blocks instantiation (time.py:39). DND_TASK is the task-property (s5p4 = enum-only per instructions); the time entity reads `device.status.dnd` which is never populated on g2408. Dead code. |
| time.py | `dnd_end` | `time.dreame_a2_mower_dnd_end` | `key="dnd_end"` → `device.status.dnd`; same gate as `dnd_start` | CFG.DND (same — not observed on g2408; capability gate blocks entity) | DELETE | — | — | — | Same reasoning as `dnd_start`: `capability.dnd = False` on g2408 (types.py:1166); never instantiated. DND is in CFG catalog but DND_TASK property is enum-only (per task spec) and `status.dnd` is never set to `True` on g2408. |
| time.py | `off_peak_charging_start` | `time.dreame_a2_mower_off_peak_charging_start` | `key="off_peak_charging_start"` → `device.status.off_peak_charging`; property `DreameMowerProperty.OFF_PEAK_CHARGING` s3.3; gated by `capability.off_peak_charging` | none (s3p3 not in §2.1 catalog; `capability.off_peak_charging = False` on g2408 — types.py:1172) | DELETE | — | — | — | `capability.off_peak_charging = False` on g2408 (types.py:1172); `exists_fn` blocks instantiation (time.py:53). s3p3 has no §2.1 entry and no observed MQTT on g2408. Dead code. |
| time.py | `off_peak_charging_end` | `time.dreame_a2_mower_off_peak_charging_end` | `key="off_peak_charging_end"` → `device.status.off_peak_charging`; same gate | none (same — `capability.off_peak_charging = False`) | DELETE | — | — | — | Same reasoning as `off_peak_charging_start`: capability gate (types.py:1172) ensures this never registers on g2408. No §2.1 evidence. |

## Section 1.3 — button.py

> **Row count**: 19 descriptors (16 in `BUTTONS` tuple + `DreameMowerFinalizeSessionButton` standalone class + `DreameMowerShortcutButtonEntity` dynamic class + `DreameMowerMapButtonEntity` dynamic class).
> The spec estimated ~27; actual count is 19 distinct descriptor shapes. The gap is partly because several vacuum-only resets (RESET_SILVER_ION, RESET_LENSBRUSH, RESET_SQUEEGEE, RESET_SENSOR, RESET_TANK_FILTER) were already removed in pre-P1 cleanup, and Take Picture (op=401) was removed in alpha.164 (comment at button.py:350–358 confirms removal).

| File | Class/key | Computed entity_id | Underlying property/action | §2.1 citation | Classification | Proposed entity_id | Proposed entity_category | Proposed enabled_by_default | Reason / notes |
|---|---|---|---|---|---|---|---|---|---|
| button.py | `start_mowing` | `button.dreame_a2_mower_start_mowing` | `device.start_mowing()` → routed-action s2 aiid=50 `m:'a' o:100` | s2.50 op=100 (confirmed, globalMower — §2.1 opcode table) | PRODUCTION | — | `None` | `True` | Primary all-area mow trigger; op=100 confirmed on g2408 in §2.1 opcode catalog (protocol doc line 1084). Renamed "Start All-Area Mowing" in alpha.155+ to distinguish from zone/spot buttons. Availability gate greys it during an active running task while not paused/returning (button.py:171–175). |
| button.py | `start_edge_mowing` | `button.dreame_a2_mower_start_edge_mowing` | `device.call_action_opcode(_OP_EDGE_MOWER)` → op=101 (no zone scope — mows full perimeter) | s2.50 op=101 (confirmed, edgeMower — §2.1 opcode table; confirmed via HA button press 2026-04-27 09:42:58 with zero BT connectivity) | PRODUCTION | — | `None` | `True` | Full-perimeter edge mow; op=101 confirmed on g2408 (protocol doc line 997: "confirmed 2026-04-27 09:42:58 via cloud routed-action siid:2 aiid:50 op:101 with zero BT connectivity"). No zone-scope payload = mow entire perimeter. Availability gate requires no active task (button.py:187–190). |
| button.py | `start_selected_zone_mow` | `button.dreame_a2_mower_start_selected_zone_mow` | `_fire_zone_mow_selection(device)` → `device.call_action_opcode(_OP_ZONE_MOWER, {"region": selection})` → op=102 | s2.50 op=102 (confirmed, zoneMower with `region:[zone_id]` payload — described in §2.1 opcode catalog alongside op=100/101) | PRODUCTION | — | `None` | `True` | Zone-scoped mow; op=102 with `region:[id,…]` payload confirmed on g2408 (protocol doc §2.1 action table). Disabled when `_zone_mow_selection` is empty so no spurious calls are fired (button.py:203–208). Pairs with switch.py zone-selection toggles. |
| button.py | `start_selected_spot_mow` | `button.dreame_a2_mower_start_selected_spot_mow` | `_fire_spot_mow_selection(device)` → `device.call_action_opcode(_OP_SPOT_MOWER, {"region": selection})` → op=103 | s2.50 op=103 (confirmed, spotMower — §2.1 opcode catalog; confirmed: "a complete 7-minute spot mow on 2026-04-26 21:49–21:57" in protocol doc line 1016) | PRODUCTION | — | `None` | `True` | Spot-scoped mow; op=103 confirmed on g2408 (protocol doc line 1016: "launch went through routed-action(siid:2 aiid:50 m:'a' o:103)"). Disabled when `_spot_mow_selection` is empty (button.py:215–220). Pairs with switch.py spot-selection toggles. |
| button.py | `start_selected_edge_mow` | `button.dreame_a2_mower_start_selected_edge_mow` | `_fire_edge_mow_selection(device)` → `device.call_action_opcode(_OP_EDGE_MOWER, {"region": selection})` → op=101 with zone-scope payload | s2.50 op=101 (confirmed, edgeMower — same opcode as full perimeter but with `region:[zone_id]` to scope to a specific zone's perimeter; §2.1 table; Dreame app "Edge" mode confirmed to prompt zone selection 2026-04-27) | PRODUCTION | — | `None` | `True` | Zone-scoped edge mow; same op=101 as `start_edge_mowing` but carries a `region` payload to restrict to one zone's perimeter. App behaviour confirmed at protocol doc line 87–91 (button.py comment: "The Dreame app's 'Edge' mode prompts the user to pick which zone's perimeter to mow — confirmed 2026-04-27 via UI inspection"). Disabled when `_edge_mow_selection` empty (button.py:229–234). |
| button.py | `pause_mowing` | `button.dreame_a2_mower_pause_mowing` | `device.pause()` → platform Pause action → s2 aiid=50 Pause opcode | Pause (confirmed — §2.1 "Confirmed routed actions: Pause / Resume / Dock / Recharge") | PRODUCTION | — | `None` | `True` | Pause is a confirmed native lawn_mower platform action on g2408. Availability gate permits only during active non-paused non-returning mow (button.py:243–246). |
| button.py | `dock` | `button.dreame_a2_mower_dock` | `device.return_to_base()` → platform Dock/Return action | Dock/Recharge (confirmed — §2.1 "Confirmed routed actions: Pause / Resume / Dock / Recharge") | PRODUCTION | — | `None` | `True` | Return-to-dock is a confirmed native lawn_mower platform action on g2408. Key kept as "dock" for backward-compat; user-facing name "Recharge" matches Dreame app wording (button.py:249–253). |
| button.py | `start_learning_map` | `button.dreame_a2_mower_start_learning_map` | `device.call_action_opcode(_OP_START_LEARNING_MAP)` → op=110 (BUILDING mode) | s2.50 op=110 (partial — op=110 triggers BUILDING mode s2p1=11, which is confirmed on g2408 at protocol doc line 564/617: "s2p1=11 … confirmed 2026-04-20 17:00:09 when user triggered 'Expand Lawn'"; however op=110 itself is not explicitly named in the §2.1 confirmed-opcode table, inferred from apk) | OBSERVABILITY | — | `EntityCategory.CONFIG` | `True` | BUILDING mode (map-learning/Expand Lawn) is confirmed on g2408 (protocol doc §4.3). Op=110 is inferred from apk rather than directly observed as a cloud command in session logs, so partial. Already `entity_category=CONFIG` (button.py:270). Essential setup step but not a regular operational button, hence OBSERVABILITY rather than PRODUCTION. |
| button.py | `stop_mowing` | `button.dreame_a2_mower_stop_mowing` | `device.stop()` → platform Stop action; adaptive label via `name_fn` | Stop (confirmed — §2.1 "Confirmed routed actions: Pause / Resume / Dock / Recharge") | PRODUCTION | — | `None` | `True` | Stop is a confirmed native lawn_mower platform action on g2408. Dynamic label (button.py:298–299) correctly follows current activity ("Stop Mowing", "Stop Returning", "Stop Patrolling", etc.) per `_active_action_label()`. |
| button.py | `RESET_BLADES` (action_key) | `button.dreame_a2_mower_reset_blades` | `DreameMowerAction.RESET_BLADES` → s9 aiid=1 | enum-only (s9 aiid=1 is in the action mapping table at types.py:825 but there is no §2.1 protocol observation of this action succeeding on g2408; no s9 property has been confirmed in any MQTT capture; exists_fn gated on `device.status.blades_life is not None` — BLADES_LEFT s9p2 itself has not been confirmed as a polled property) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Blade consumable reset. BLADES_LEFT (s9p2) and the reset action (s9 aiid=1) are in the enum/mapping tables but have no confirmed MQTT evidence on g2408 (no s9 property appears in any captured session). The exists_fn gate (button.py:304–307) will prevent instantiation unless `blades_life` is non-None, so unlikely to appear in practice. Treat as EXPERIMENTAL: keep but disable by default pending a confirmed s9p2 observation. |
| button.py | `RESET_SIDE_BRUSH` (action_key) | `button.dreame_a2_mower_reset_side_brush` | `DreameMowerAction.RESET_SIDE_BRUSH` → s10 aiid=1 | enum-only (same rationale as RESET_BLADES — s10 aiid=1 and s10p2 exist in mapping table but no g2408 MQTT observation; exists_fn gated on `side_brush_life is not None`) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Side brush consumable reset; same evidence gap as RESET_BLADES. s10 properties unconfirmed on g2408. exists_fn gate (button.py:313–316) prevents instantiation unless s10p2 is actually polled. EXPERIMENTAL pending s10p2 observation. Note: the A2 g2408 has a side brush (unlike some mower models), so this *could* be real hardware — just not yet confirmed via MQTT. |
| button.py | `RESET_FILTER` (action_key) | `button.dreame_a2_mower_reset_filter` | `DreameMowerAction.RESET_FILTER` → s11 aiid=1 | enum-only (s11 aiid=1 and s11p1 are in the mapping table but no g2408 MQTT observation; the task spec explicitly names RESET_FILTER as a vacuum-only DELETE candidate; no filter consumable on a lawn mower) | DELETE | — | — | — | Vacuum-only consumable. The g2408 is a lawn mower with no filter — this is a direct carry-over from the upstream vacuum codebase. Task spec §"Vacuum-only actions" explicitly lists RESET_FILTER (s4 aiid=20 in vacuum spec; here mapped to s11 aiid=1 but same semantic category). exists_fn gate (button.py:320–324) requires `filter_life is not None`; on g2408 s11p1 is never polled so the entity should never instantiate, but the code should be removed to eliminate confusion. |
| button.py | `find_bot` | `button.dreame_a2_mower_find_bot` | `device.call_action_opcode(_OP_FIND_BOT)` → op=9 | s2.50 op=9 (confirmed — appears in §2.1 routed-action opcode table at protocol doc line 1084: "100 globalMower, 101 edgeMower, 9 findBot, 11 suppressFault, 12 lockBot…") | PRODUCTION | — | `None` | `True` | Find Mower (audible beep to locate the mower in long grass). Op=9 is listed as a confirmed routed-action opcode in the §2.1 protocol table. User-actionable utility: confirmed on the same s2 aiid=50 envelope as other opcodes. |
| button.py | `lock_bot` | `button.dreame_a2_mower_lock_bot` | `device.call_action_opcode(_OP_LOCK_BOT)` → op=12 | s2.50 op=12 (confirmed — appears in §2.1 routed-action opcode table at protocol doc line 1084: "12 lockBot") | PRODUCTION | — | `None` | `True` | Lock Mower (anti-theft / child-lock). Op=12 is listed in the §2.1 confirmed-opcode table. User-actionable security feature. |
| button.py | `suppress_fault` | `button.dreame_a2_mower_suppress_fault` | `device.call_action_opcode(_OP_SUPPRESS_FAULT)` → op=11 | s2.50 op=11 (confirmed — appears in §2.1 routed-action opcode table at protocol doc line 1084: "11 suppressFault") | PRODUCTION | — | `None` | `True` | Clear Warning (dismiss non-critical fault). Op=11 listed in confirmed §2.1 opcode table. User-actionable: clears warning state so mowing can resume. |
| button.py | `cutter_bias` | `button.dreame_a2_mower_cutter_bias` | `device.call_action_opcode(_OP_CUTTER_BIAS)` → op=503 | s2.50 op=503 (partial — appears in §2.1 opcode catalog at protocol doc line 1084: "503 cutterBias …" with ellipsis suggesting it's catalogued but not fully characterised; no g2408 session log shows this opcode being issued or echoed) | EXPERIMENTAL | — | `EntityCategory.CONFIG` | `False` | Calibrate Blade (cutter-height or cutter-phase calibration). Op=503 appears in the §2.1 opcode catalog with a trailing "…" suggesting partial characterisation. No confirmed successful invocation on g2408. Keep but disable by default pending a live confirmation. Already lacks an entity_category assignment in current code (button.py:361–366); recommend adding `EntityCategory.CONFIG`. |
| button.py | `DreameMowerFinalizeSessionButton` | `button.<device_name>_finalize_session` (unique_id: `<mac>_finalize_session`) | HA-internal `live_map.finalize_session()` — no MQTT; promotes in-progress aggregator to "(incomplete)" archive entry | none (HA-only — operates on the integration's on-disk session archive, not a device MQTT property; no siid/aiid call) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Emergency session-close button confirmed working (alpha.163+). Forces closure of a stuck in-progress session entry when the auto-close MQTT signal was missed (HA down during mow end, mower offline at dock-up, etc). Already `entity_category=CONFIG` (button.py:658). Available only when `archive.in_progress_entry()` is non-None (button.py:683). No §2.1 citation needed — purely HA-side recovery tool. |
| button.py | `DreameMowerShortcutButtonEntity` (key="shortcut") | `button.<device_name>_shortcut_<N>` (unique_id: `<mac>_shortcut_<N>`) | `device.start_shortcut(shortcut_id)` — routed action to start a named shortcut sequence; gated by `capability.shortcuts = False` on g2408 | none (`capability.shortcuts = False` on g2408, types.py:1168; entity never instantiates; shortcut feature is vacuum-era — no shortcut sequences defined in g2408 app) | DELETE | — | — | — | `capability.shortcuts = False` on g2408 (types.py:1168); `async_setup_entry` gate at button.py:404 blocks instantiation. Shortcuts are a vacuum-platform feature (pre-programmed cleaning sequences); no g2408 evidence. Dead code on g2408 that should be removed to reduce maintenance surface. |
| button.py | `DreameMowerMapButtonEntity` (key="backup") | `button.<device_name>_backup_map_<N>` (unique_id: `<mac>_backup_map_<N>`) | `device.backup_map(map_id)` — triggers a map backup/snapshot; gated by `capability.backup_map = False` on g2408 | none (`capability.backup_map = False` on g2408, types.py:1165; entity never instantiates; map-backup is a vacuum multi-floor feature absent on g2408 which has a single fixed lawn map) | DELETE | — | — | — | `capability.backup_map = False` on g2408 (types.py:1165); `async_setup_entry` gate at button.py:430 blocks instantiation. Map backup/restore is a vacuum multi-floor-plan feature. g2408 has one lawn map managed via BUILDING sessions, not a backup-and-restore model. Dead code on g2408 that should be removed. |

## Section 1.4 — switch.py

> **Row count**: 35 rows total — 32 descriptors in the `SWITCHES` tuple + `DreameMowSelectionSwitch` dynamic class (3 variants: zone / spot / edge). The spec estimated ~47; actual is lower because several blocks of AI sub-switches and AutoSwitch sub-switches share a single descriptor each rather than being fanned out into per-bit rows. Dynamic selection switches are per-map-area but represented as 3 rows (zone / spot / edge kind).

| File | Class/key | Computed entity_id | Underlying property/action | §2.1 citation | Classification | Proposed entity_id | Proposed entity_category | Proposed enabled_by_default | Reason / notes |
|---|---|---|---|---|---|---|---|---|---|
| switch.py | `RESUME_CLEANING` | `switch.dreame_a2_mower_resume_cleaning` | `property_key=DreameMowerProperty.RESUME_CLEANING` → s4p11; no `exists_fn` → always instantiates | none (s4p11 not in §2.1 catalog; never observed in 3 weeks of MQTT captures; not listed in capability snapshot) | DELETE | — | — | — | s4p11 is not in §2.1 and never appears in any g2408 MQTT session. No `exists_fn` gate so the entity instantiates unconditionally, but any write would silently no-op (the property doesn't exist on this firmware). Vacuum-era resume-cleaning setting; on g2408 mow resumption after mid-session recharge is governed by the session state machine, not a toggleable property. |
| switch.py | `OBSTACLE_AVOIDANCE` | `switch.dreame_a2_mower_obstacle_avoidance` | `property_key=DreameMowerProperty.OBSTACLE_AVOIDANCE` → s4p21; no `exists_fn` → always instantiates | none (s4p21 = OBSTACLE_AVOIDANCE listed as "never observed" in capability snapshot, types.py:687; not in §2.1 protocol catalog) | DELETE | — | — | — | s4p21 is in the capability snapshot's "never observed / enum-only" list (snapshot doc line 43: "s4p21 (OBSTACLE_AVOIDANCE) — also drove `disable_sensor_cleaning`"). No `exists_fn` gate; entity shows up on g2408 but any write silently fails because the firmware doesn't acknowledge this property. On g2408 the equivalent functionality is human-presence detection via the HUMAN_PRESENCE_ALERT CFG.REC path (s2p51), not a direct property toggle. |
| switch.py | `CUSTOMIZED_CLEANING` | `switch.dreame_a2_mower_customized_cleaning` | `property_key=DreameMowerProperty.CUSTOMIZED_CLEANING` → s4p26; no `exists_fn` | none (s4p26 = CUSTOMIZED_CLEANING listed as "never observed" in capability snapshot; types.py:691) | DELETE | — | — | — | s4p26 is in the capability snapshot's "never observed" list (snapshot doc line 37: "s4p26 (CUSTOMIZED_CLEANING)"). No `exists_fn` gate; entity instantiates and silently no-ops on write. Zone-customized-cleaning on g2408 is handled by zone-scoped mow opcodes (op=102), not a generic cleaning-order property. |
| switch.py | `CHILD_LOCK` | `switch.dreame_a2_mower_child_lock` | `property_key=DreameMowerProperty.CHILD_LOCK` → s4p27; no `exists_fn` | CFG.CLS (confirmed 2026-04-24 — protocol doc line 1126: "Child Lock confirmed 2026-04-24. Mapping {0: off, 1: on}. Surfaced as sensor.child_lock_cfg.") | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Child-lock is a confirmed real setting on g2408 via CFG.CLS read path (protocol doc §6 table). Write goes to s4p27; the protocol doc notes "A switch.child_lock entity already exists wired to DreameMowerProperty.CHILD_LOCK, but on g2408 the authoritative read path is CFG.CLS" — so reading may show stale/None if the s4p27 push is not observed, but the write is likely honored. User-actionable: prevents mower from responding to accidental commands. Concern: read-back discrepancy means the switch state may be inaccurate until CFG.CLS is wired as the value source. Flag for P2.3. |
| switch.py | `dnd` (key="dnd") | `switch.dreame_a2_mower_dnd` | `key="dnd"` → `device.set_dnd(value)` → writes to `DreameMowerProperty.DND` s5p1 or `DND_TASK` s5p4; reads `device.status.dnd` which requires `capability.dnd = True`; no `exists_fn` — always instantiates | CFG.DND (in §2.1 catalog, protocol doc line 1120: "Do-not-disturb (apk-catalogued). Sample [0, 21:00, 07:00] = off"; but DND_TASK s5p4 listed as "never observed" in capability snapshot) | DELETE | — | — | — | `capability.dnd = False` on g2408 (types.py:1166) and `capability.dnd_task = False` (types.py:1167). `status.dnd` returns `None` when `capability.dnd = False` (device.py:7669 guard). Despite no `exists_fn`, the switch instantiates, always reads `None` (displayed as Off), and any write calls `set_dnd()` which falls through to `set_property(DreameMowerProperty.DND, ...)` → s5p1 — a property that is not in §2.1 and has never been observed. CFG.DND is read-only data available via `getCFG`; the actual MQTT write transport is unconfirmed. This is the `time.dnd_start`/`time.dnd_end` DELETE scenario re-confirmed for the boolean toggle. |
| switch.py | `MULTI_FLOOR_MAP` | `switch.dreame_a2_mower_multi_floor_map` | `property_key=DreameMowerProperty.MULTI_FLOOR_MAP` → s6p7; `exists_fn` checks `lidar_navigation = True` → entity **does** instantiate on g2408 | none (s6p7 = MULTI_FLOOR_MAP listed as "never observed" in capability snapshot: "s6p7 (MULTI_FLOOR_MAP)"; `capability.multi_floor_map = False` (types.py:1160)) | DELETE | — | — | — | Although `exists_fn` uses `lidar_navigation = True` (so the entity instantiates), s6p7 is in the "never observed" list. `capability.multi_floor_map = False` means the integration itself knows this device doesn't support it. The mower has exactly one lawn map; multi-floor-map makes no semantic sense. Write would go to s6p7 which firmware presumably ignores. The `exists_fn` at switch.py:73 was likely copied from the LiDAR navigation guard without noticing the conflation between "has LiDAR" and "supports multiple floor plans". |
| switch.py | `INTELLIGENT_RECOGNITION` | `switch.dreame_a2_mower_intelligent_recognition` | `property_key=DreameMowerProperty.INTELLIGENT_RECOGNITION` → s4p49; `exists_fn` checks `capability.wifi_map = False` → entity **never** instantiates | none (s4p49 not in §2.1; `capability.wifi_map = False` (types.py:1164); entity blocked by exists_fn) | DELETE | — | — | — | `capability.wifi_map = False` on g2408 (types.py:1164); `exists_fn` at switch.py:82–83 blocks instantiation. s4p49 has no §2.1 entry. Dead code. |
| switch.py | `MAP_SAVING` | `switch.dreame_a2_mower_map_saving` | `property_key=DreameMowerProperty.MAP_SAVING` → s13p1; no `exists_fn` → always instantiates | none (s13p1 = MAP_SAVING listed as "never observed" in capability snapshot: "s13p1 (MAP_SAVING) — its absence drives `lidar_navigation = True`") | DELETE | — | — | — | s13p1 is never observed precisely because its absence is the signal for "this device uses LiDAR and saves the map automatically." A toggle switch for MAP_SAVING on g2408 is meaningless — LiDAR devices have always-on map saving with no user-visible toggle. No `exists_fn` gate so it instantiates unconditionally, but writes silently fail. |
| switch.py | `VOICE_ASSISTANT` | `switch.dreame_a2_mower_voice_assistant` | `property_key=DreameMowerProperty.VOICE_ASSISTANT` → s7p5; no `exists_fn` | none (s7p5 = VOICE_ASSISTANT listed as "never observed" in capability snapshot: "s7p5 (VOICE_ASSISTANT) and related"; `capability.voice_assistant = False` (types.py:1169)) | DELETE | — | — | — | s7p5 is in the capability snapshot "never observed" list. `capability.voice_assistant = False` on g2408. No `exists_fn` gate, so entity instantiates and shows as Off; any write goes to s7p5 which firmware apparently ignores. Robot voice is controlled via CFG.VOL (volume, confirmed) and CFG.VOICE (voice prompts, confirmed) — not a binary enable/disable property at s7p5. |
| switch.py | `cleaning_sequence` (key) | `switch.dreame_a2_mower_cleaning_sequence` | `value_fn` → `device.status.custom_order`; `set_fn` → `device.set_cleaning_sequence()`; `exists_fn` checks `capability.customized_cleaning and capability.map` | none (`capability.customized_cleaning = False` on g2408 (types.py:1162); `exists_fn` at switch.py:101 blocks instantiation) | DELETE | — | — | — | `capability.customized_cleaning = False` on g2408 (types.py:1162); `exists_fn` blocks entity registration. Vacuum-era zone-ordering feature; on g2408 zone sequencing is implicit in the mowingAreas order within the s2p50 op=102 payload. Dead code. |
| switch.py | `AI_OBSTACLE_DETECTION` (DreameMowerAIProperty) | `switch.dreame_a2_mower_ai_obstacle_detection` | `property_key=DreameMowerAIProperty.AI_OBSTACLE_DETECTION` (bit 2) → transport via `set_ai_property()` → `set_ai_detection()` → s4p22; gated internally by `capability.ai_detection = False`; no `exists_fn` | none (s4p22 = AI_DETECTION listed as "never observed" in capability snapshot (snapshot doc line 37); `capability.ai_detection = False` (types.py:1161)) | DELETE | — | — | — | s4p22 is in the "never observed" list. `capability.ai_detection = False` (types.py:1161) means `get_ai_property()` always returns `None` and `set_ai_property()` no-ops (device.py:5225 guard: "if self.capability.ai_detection:"). No `exists_fn` gate so entity instantiates but is permanently broken. The g2408 is a lawn mower with no camera obstacle-detection hardware. All seven AI/StrAI sub-switches sharing the s4p22 transport are DELETE for the same reason. |
| switch.py | `AI_OBSTACLE_IMAGE_UPLOAD` (DreameMowerAIProperty) | `switch.dreame_a2_mower_ai_obstacle_image_upload` | `property_key=DreameMowerAIProperty.AI_OBSTACLE_IMAGE_UPLOAD` (bit 32) → s4p22 via `set_ai_property()`; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | Same parent transport as `AI_OBSTACLE_DETECTION` (s4p22, never observed, capability.ai_detection=False). Entity instantiates, always reads None, any write is no-op. Vacuum-era obstacle-photo-upload feature; lawn mower has no camera. |
| switch.py | `AI_OBSTACLE_PICTURE` (DreameMowerAIProperty) | `switch.dreame_a2_mower_ai_obstacle_picture` | `property_key=DreameMowerAIProperty.AI_OBSTACLE_PICTURE` (bit 4) → s4p22; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | Same parent transport as `AI_OBSTACLE_DETECTION`. Entity instantiates, always reads None, any write is no-op. Vacuum-era AI picture feature; no camera on g2408. |
| switch.py | `AI_PET_DETECTION` (DreameMowerAIProperty) | `switch.dreame_a2_mower_ai_pet_detection` | `property_key=DreameMowerAIProperty.AI_PET_DETECTION` (bit 16) → s4p22; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | Same parent transport as `AI_OBSTACLE_DETECTION`. No camera on g2408. `PET_DETECTIVE` (s4p59) also "never observed" per capability snapshot (line 37). Permanently broken entity. |
| switch.py | `AI_HUMAN_DETECTION` (DreameMowerStrAIProperty) | `switch.dreame_a2_mower_ai_human_detection` | `property_key=DreameMowerStrAIProperty.AI_HUMAN_DETECTION` (JSON key "human_detect_switch") → s4p22 JSON-string write via `set_ai_detection({...})`; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | StrAI properties share the same s4p22 transport as AiProperty bitmask, but use JSON dict format (device.py:5241: "result = self.set_ai_detection({DreameMowerStrAIProperty[prop.name].value: bool(value)})"). Still gated by `capability.ai_detection = False`. Entity instantiates but permanently broken. Note: human-presence detection on g2408 is a separate confirmed feature via CFG.REC / s2p51, NOT via s4p22. |
| switch.py | `FUZZY_OBSTACLE_DETECTION` (DreameMowerAIProperty) | `switch.dreame_a2_mower_fuzzy_obstacle_detection` | `property_key=DreameMowerAIProperty.FUZZY_OBSTACLE_DETECTION` (bit 256) → s4p22; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | Same parent transport as `AI_OBSTACLE_DETECTION`. Permanently broken. |
| switch.py | `AI_PET_AVOIDANCE` (DreameMowerAIProperty) | `switch.dreame_a2_mower_ai_pet_avoidance` | `property_key=DreameMowerAIProperty.AI_PET_AVOIDANCE` (bit 128) → s4p22; `exists_fn` checks `capability.pet_detective = False` → entity **never** instantiates | none (s4p22 never observed; `capability.pet_detective = False` (types.py:1171); `exists_fn` blocks registration) | DELETE | — | — | — | Doubly blocked: `capability.ai_detection = False` AND `capability.pet_detective = False` (types.py:1171) with an `exists_fn` guard (switch.py:147). Entity never registers. Dead code. |
| switch.py | `PET_PICTURE` (DreameMowerAIProperty) | `switch.dreame_a2_mower_pet_picture` | `property_key=DreameMowerAIProperty.PET_PICTURE` (bit 512) → s4p22; no `exists_fn` | none (s4p22 never observed; `capability.ai_detection = False`) | DELETE | — | — | — | Same parent transport as `AI_OBSTACLE_DETECTION`. No `exists_fn` gate, entity instantiates but permanently broken; any write is no-op due to `capability.ai_detection = False` guard in `set_ai_property()`. |
| switch.py | `PET_FOCUSED_DETECTION` (DreameMowerAIProperty) | `switch.dreame_a2_mower_pet_focused_detection` | `property_key=DreameMowerAIProperty.PET_FOCUSED_DETECTION` (bit 1024) → s4p22; `exists_fn` checks `capability.pet_furniture = False` → entity **never** instantiates | none (s4p22 never observed; `capability.pet_furniture = False` (types.py:1186); `exists_fn` at switch.py:158 blocks registration) | DELETE | — | — | — | `exists_fn` gate (capability.pet_furniture=False) prevents registration. Also doubly blocked by `capability.ai_detection = False`. Dead code. |
| switch.py | `FILL_LIGHT` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_fill_light` | `property_key=DreameMowerAutoSwitchProperty.FILL_LIGHT` ("FillinLight") → s4p54 AUTO_SWITCH_SETTINGS sub-field; `exists_fn` checks `capability.fill_light = False` → entity **never** instantiates | none (s4p54 = AUTO_SWITCH_SETTINGS listed as "never observed" in capability snapshot; `capability.fill_light = False` (types.py:1169)) | DELETE | — | — | — | `capability.fill_light = False` on g2408 (types.py:1169); `exists_fn` at switch.py:163–165 blocks registration. s4p54 is also never observed. Dead code. |
| switch.py | `COLLISION_AVOIDANCE` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_collision_avoidance` | `property_key=DreameMowerAutoSwitchProperty.COLLISION_AVOIDANCE` ("LessColl") → s4p54 AUTO_SWITCH_SETTINGS; no `exists_fn` → always instantiates | none (s4p54 = AUTO_SWITCH_SETTINGS "never observed" in capability snapshot; `capability.auto_switch_settings = False` (types.py:1163)) | DELETE | — | — | — | No `exists_fn` gate so entity instantiates. However `get_auto_switch_property()` returns `None` when `capability.auto_switch_settings = False` (device.py:3452), so read always None; `set_auto_switch_property()` is a no-op for same reason (device.py:5273). Entity shows as Off and any toggle is silently discarded. s4p54 never observed on g2408. |
| switch.py | `STAIN_AVOIDANCE` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_stain_avoidance` | `property_key=DreameMowerAutoSwitchProperty.STAIN_AVOIDANCE` ("StainIdentify") → s4p54; `exists_fn` checks `capability.fluid_detection = False` → entity **never** instantiates | none (s4p54 never observed; `capability.fluid_detection = False` (types.py:1183)) | DELETE | — | — | — | `capability.fluid_detection = False` on g2408 (types.py:1183); `exists_fn` at switch.py:178–180 blocks registration. s4p54 never observed. Dead code. |
| switch.py | `FLOOR_DIRECTION_CLEANING` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_floor_direction_cleaning` | `property_key=DreameMowerAutoSwitchProperty.FLOOR_DIRECTION_CLEANING` ("MaterialDirectionClean") → s4p54; `exists_fn` checks `capability.floor_direction_cleaning = False` → entity **never** instantiates | none (s4p54 never observed; `capability.floor_direction_cleaning = False` (types.py:1178)) | DELETE | — | — | — | `capability.floor_direction_cleaning = False` on g2408 (types.py:1178); `exists_fn` at switch.py:185–187 blocks registration. Vacuum-era feature. Dead code. |
| switch.py | `PET_FOCUSED_CLEANING` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_pet_focused_cleaning` | `property_key=DreameMowerAutoSwitchProperty.PET_FOCUSED_CLEANING` ("PetPartClean") → s4p54; `exists_fn` checks `capability.pet_detective = False` → entity **never** instantiates | none (s4p54 never observed; `capability.pet_detective = False` (types.py:1171)) | DELETE | — | — | — | `capability.pet_detective = False` on g2408 (types.py:1171); `exists_fn` at switch.py:195 blocks registration. Dead code. |
| switch.py | `GAP_CLEANING_EXTENSION` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_gap_cleaning_extension` | `property_key=DreameMowerAutoSwitchProperty.GAP_CLEANING_EXTENSION` ("LacuneMopScalable") → s4p54; `exists_fn` returns `True` → always instantiates | none (s4p54 = AUTO_SWITCH_SETTINGS "never observed"; `capability.auto_switch_settings = False` (types.py:1163)) | DELETE | — | — | — | `exists_fn` always returns True (switch.py:203–204) so entity instantiates on g2408. But `capability.auto_switch_settings = False` means read returns None and write is no-op (same as COLLISION_AVOIDANCE above). Additionally "LacuneMopScalable" is a vacuum mop-scalable-gap feature (for cleaning furniture legs) — semantically nonsensical on a lawn mower. DELETE on both protocol and semantic grounds. |
| switch.py | `OFF_PEAK_CHARGING` | `switch.dreame_a2_mower_off_peak_charging` | `property_key=DreameMowerProperty.OFF_PEAK_CHARGING` → s3p3; no `exists_fn` → always instantiates | none (s3p3 = OFF_PEAK_CHARGING listed as "never observed" in capability snapshot (line 44: "s3p3 (OFF_PEAK_CHARGING)"); `capability.off_peak_charging = False` (types.py:1176)) | DELETE | — | — | — | s3p3 is in the capability snapshot "never observed" list. No `exists_fn` gate, entity instantiates and is silently broken. Note: the charging configuration on g2408 is confirmed via CFG.BAT (protocol doc line 1131: "Charging config — confirmed 2026-04-24") which carries recharge_pct, resume_pct, custom_charging schedule — a richer structure than a binary off-peak toggle. |
| switch.py | `AUTO_CHARGING` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_auto_charging` | `property_key=DreameMowerAutoSwitchProperty.AUTO_CHARGING` ("SmartCharge") → s4p54; `exists_fn` checks `capability.auto_charging = False` → entity **never** instantiates | none (s4p54 never observed; `capability.auto_charging = False` (types.py:1194)) | DELETE | — | — | — | `capability.auto_charging = False` on g2408 (types.py:1194); `exists_fn` at switch.py:216–218 blocks registration. s4p54 never observed. Dead code. |
| switch.py | `HUMAN_FOLLOW` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_human_follow` | `property_key=DreameMowerAutoSwitchProperty.HUMAN_FOLLOW` ("MonitorHumanFollow") → s4p54; `exists_fn` returns `True` → always instantiates | none (s4p54 = AUTO_SWITCH_SETTINGS "never observed"; `capability.auto_switch_settings = False`) | DELETE | — | — | — | `exists_fn` always returns True (switch.py:225) so entity instantiates. But `capability.auto_switch_settings = False` means permanently broken (read=None, write no-op). "MonitorHumanFollow" refers to camera-tracking/follow-me features of the STREAM service (s10001 STREAM_* family) — never observed on g2408 and conceptually inapplicable to a lawn mower. |
| switch.py | `STREAMING_VOICE_PROMPT` (DreameMowerAutoSwitchProperty) | `switch.dreame_a2_mower_streaming_voice_prompt` | `property_key=DreameMowerAutoSwitchProperty.STREAMING_VOICE_PROMPT` ("MonitorPromptLevel") → s4p54; `exists_fn` checks `capability.camera_streaming = False` → entity **never** instantiates | none (s4p54 never observed; `capability.camera_streaming = False` (types.py:1196)) | DELETE | — | — | — | `capability.camera_streaming = False` on g2408 (types.py:1196); `exists_fn` at switch.py:233 blocks registration. Camera-streaming feature absent on g2408. Dead code. |
| switch.py | `camera_light_brightness_auto` (key) | `switch.dreame_a2_mower_camera_light_brightness_auto` | `value_fn` → `device.status.camera_light_brightness == 101` (s10001p9 CAMERA_LIGHT_BRIGHTNESS); `exists_fn` checks `capability.camera_streaming and capability.fill_light` — both False → entity **never** instantiates | none (s10001p9 = CAMERA_LIGHT_BRIGHTNESS listed as "never observed" in capability snapshot (line 40); both capability flags False) | DELETE | — | — | — | `exists_fn` at switch.py:241–242 requires both `capability.camera_streaming = False` AND `capability.fill_light = False` — neither is True on g2408. Entity never registers. Dead code. |
| switch.py | `mow_mode_efficient` (key) | `switch.dreame_a2_mower_mow_mode_efficient` | `value_fn` → `device.cfg.get("PRE", ...)[1] == 1`; `set_fn` → `device.write_pre(1, ...)`; `exists_fn` → `False` — never instantiates | CFG.PRE[1] (confirmed — protocol doc line 1098: "PRE[1]: mode (0=Standard, 1=Efficient)"; but switch.py comment (line 246–252) explains write path is wrong: "toggling Standard ↔ Efficient in the app re-emits s6p2 … but PRE stays unchanged") | EXPERIMENTAL | — | `EntityCategory.CONFIG` | `False` | `exists_fn` hard-coded to `False` (switch.py:265) so entity never registers in current code. CFG.PRE[1] is confirmed as the mode storage (protocol doc line 1098) but the write transport for changing it is unknown — the comment correctly identifies that `write_pre(1, …)` doesn't work on g2408. Read also unreliable (PRE[1] always shows old value after toggle). Flag as EXPERIMENTAL: keep the entity (the read logic via CFG is partially correct) but leave `exists_fn=False` until the correct write transport is identified (possibly a dedicated `s PRE` routed action). Protocol doc line 1083: "s PRE — Write 10-element preferences array (read-modify-write)" suggests a write path exists but needs confirmation. |
| switch.py | `show_spot_zones` (key) | `switch.dreame_a2_mower_show_spot_zones` | `value_fn` → `device._show_spot_zones_overlay`; `set_fn` → `_set_spot_zones_overlay()`; `exists_fn` → `True` — always instantiates; HA-only display toggle | none (HA-only — no MQTT property; controls map camera rendering flag `_show_spot_zones_overlay`; triggers `_build_map_from_cloud_data()` rebuild on toggle) | PRODUCTION | — | `EntityCategory.CONFIG` | `False` | HA-only display preference: toggles whether spotAreas are drawn on the live-map camera. No device communication. Confirmed working in alpha.163+ (the `_set_spot_zones_overlay` helper at switch.py:286–297 triggers immediate camera rebuild). `enabled_by_default=False` is correct — spot-zone overlay is a power-user / debugging aid; most users will want the cleaner map view without spot markers. |
| switch.py | `DreameMowSelectionSwitch` (kind="zone") | `switch.<device_name>_<zone_name>_mow_next` (unique_id: `<mac>_zone_select_<id>`) | HA-only: reads/writes `device._zone_mow_selection` list; ON = zone id in selection list; fires when user presses "Start Selected Zone Mow" button | s2.50 op=102 (confirmed — zone mow selection feeds the `region` payload of op=102, per §2.1 opcode catalog and button.py:204) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Per-zone "include in next mow" toggle. Selection is consumed by `start_selected_zone_mow` button → op=102 with `region:[zone_id,…]`. Both op=102 and the zone mow flow are confirmed on g2408 (protocol doc §2.1 opcode catalog). Dynamically created at setup time from `mowingAreas` in the cloud map payload (switch.py:322–346). `enabled_by_default=True` appropriate — the zone toggles are the primary way to initiate partial mows. |
| switch.py | `DreameMowSelectionSwitch` (kind="spot") | `switch.<device_name>_<spot_name>_mow_next` (unique_id: `<mac>_spot_select_<id>`) | HA-only: reads/writes `device._spot_mow_selection` list; ON = spot id in selection; consumed by "Start Selected Spot Mow" button → op=103 | s2.50 op=103 (confirmed — "a complete 7-minute spot mow on 2026-04-26 21:49–21:57" per protocol doc line 1016; section 1.3 row `start_selected_spot_mow` confirms the spot-mow flow) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Per-spot "include in next spot mow" toggle. Selection feeds `start_selected_spot_mow` button → op=103 with `region:[spot_id,…]`. op=103 confirmed on g2408 (protocol doc line 1016). Dynamically created from `spotAreas` in cloud map payload (switch.py:322–346). |
| switch.py | `DreameMowSelectionSwitch` (kind="edge") | `switch.<device_name>_<zone_name>_edge_mow_next` (unique_id: `<mac>_edge_select_<id>`) | HA-only: reads/writes `device._edge_mow_selection` list; ON = zone id in selection; consumed by "Start Selected Edge Mow" button → op=101 with zone-scope payload | s2.50 op=101 (confirmed — edge mow confirmed 2026-04-27 per protocol doc line 997; zone-scoped variant confirmed per section 1.3 `start_selected_edge_mow` row) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | Per-zone "mow this zone's perimeter next" toggle. Selection feeds `start_selected_edge_mow` button → op=101 with `region:[zone_id]`. op=101 confirmed on g2408. Dynamically created from `mowingAreas` in cloud map payload — one "Zone Edge" switch per zone (switch.py:327, label_prefix="Zone Edge"). |

## Section 1.5 — sensor.py

> **Row count**: 80 descriptors total — 78 `DreameMowerSensorEntityDescription` instances in the `SENSORS` tuple + 2 standalone sensor classes (`DreameArchivedSessionsSensor`, `DreameArchivedLidarScansSensor`).
> The spec estimated ~84; actual is 80. The gap is partly from vacuum-only consumable sensors already deleted in alpha.32 (TANK_FILTER, SILVER_ION, LENSBRUSH, SQUEEGEE — comment at sensor.py:386–393 confirms removal), partly from the `sensor.error` descriptor that was removed and replaced by dedicated binary_sensors (comment at sensor.py:297–313), and partly from single-point convenience sensors removed in alpha.113 (maintenance_point_x/y at sensor.py:1493–1497).

| File | Class/key | Computed entity_id | Underlying property/action | §2.1 citation | Classification | Proposed entity_id | Proposed entity_category | Proposed enabled_by_default | Reason / notes |
|---|---|---|---|---|---|---|---|---|---|
| sensor.py | `CLEANING_TIME` | `sensor.dreame_a2_mower_cleaning_time` | `property_key=DreameMowerProperty.CLEANING_TIME` → s4p2 (vacuum "cleaning-time" register, maps to siid=4 piid=2 per types.py:673) | none (s4p2 not in §2.1 catalog for g2408; siid=4 on g2408 is the session-event service, not the mowing-state service; s4p2 was never observed in any MQTT capture — the equivalent data comes from s1.4 MOWING_TELEMETRY `area_mowed_m2` / `distance_m` during a session and from the session-summary event_occured at end) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Vacuum-era s4p2 "cleaning time" property — no MQTT observation on g2408 in 3 weeks of probe logs. Not in §2.1. Will show as Unavailable until/unless g2408 firmware starts pushing s4p2. Keep the entity (it may work on other Dreame mower firmware variants) but disable by default pending a confirmed g2408 observation. If still unavailable after a full release cycle, candidate for DELETE. |
| sensor.py | `CLEANED_AREA` | `sensor.dreame_a2_mower_cleaned_area` | `property_key=DreameMowerProperty.CLEANED_AREA` → s4p3 (vacuum "cleaned-area" register, siid=4 piid=3 per types.py:674) | none (s4p3 not in §2.1 catalog; same siid=4 note as CLEANING_TIME; area data comes from s1.4 telemetry `area_mowed_m2` for live session and session-summary OSS JSON at end) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Same reasoning as `CLEANING_TIME` — vacuum-era s4p3 not observed on g2408. The session-scoped equivalent is `sensor.session_area_mowed` from s1.4. Disable by default; DELETE candidate if still Unavailable post-release. |
| sensor.py | `STATE` | `sensor.dreame_a2_mower_state` | `property_key=DreameMowerProperty.STATE` → s2p1 (g2408 mode enum: 1=Mowing, 2=Idle, 3=Paused, 5=Returning, 6=Charging, 11=Mapping, 13=Charged, 14=Updating) | s2.1 (confirmed — §2.1 table row "2.1 Status enum") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | s2p1 is the confirmed g2408 mow-state register and the primary protocol state signal. However the `lawn_mower` platform entity (section 1.1) exposes the user-facing activity derived from the same property — having both is redundant for users. The sensor adds value as a diagnostic surface for raw enum code visibility (e.g. distinguishing state=11 BUILDING from state=1 MOWING which both show as "mowing" in the lawn_mower entity). Keep as OBSERVABILITY under Diagnostic. |
| sensor.py | `STATUS` | `sensor.dreame_a2_mower_status` | `property_key=DreameMowerProperty.STATUS` → s4p1 (vacuum "status" sub-register, siid=4 piid=1 per types.py:672 — distinct from s2p1 STATE which is the g2408 confirmed state channel) | none (s4p1 not in §2.1 catalog for g2408; s4p1 was not observed in any g2408 MQTT capture during 3-week probe period; the main mowing state is on s2p1=STATE) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Vacuum-era s4p1 "status" sub-register, not confirmed on g2408. Distinct from s2p1 (STATE, confirmed). No §2.1 entry. Will show as Unavailable unless g2408 firmware pushes s4p1. Disable by default; the `mower_status_raw` sensor at sensor.py:1249 already exposes `device.status.status` as an int for protocol debugging (same value path). EXPERIMENTAL pending observation. |
| sensor.py | `RELOCATION_STATUS` | `sensor.dreame_a2_mower_relocation_status` | `property_key=DreameMowerProperty.RELOCATION_STATUS` → s4p20 (vacuum RELOCATION_STATUS, siid=4 piid=20 per types.py:686) | none (s4p20 not in §2.1 catalog; not in observed-property list from 3-week capture; g2408 relocalization surface is s2p65 SLAM task-type label + s2p2 code 71 — both confirmed) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Vacuum-era relocalization-status property at s4p20, not observed on g2408. SLAM relocalization on g2408 is surfaced via `sensor.slam_activity` (s2p65, confirmed) and `binary_sensor.positioning_failed` (s2p2 code 71, confirmed) — both are better-instrumented equivalents. Keep as EXPERIMENTAL: disable by default and DELETE if still Unavailable post-release. |
| sensor.py | `TASK_TYPE` | `sensor.dreame_a2_mower_task_type` | `property_key=DreameMowerProperty.TASK_TYPE` → s4p58; `exists_fn` checks `device.capability.task_type` which is `False` (types.py:1187) | none (s4p58 listed as "never observed" in capability snapshot line 37; `capability.task_type = False` → entity never registers) | DELETE | — | — | — | `capability.task_type = False` on g2408 (types.py:1187); `exists_fn` at sensor.py:289 blocks entity registration. s4p58 is in the capability snapshot "never observed" list. Dead code — entity never instantiates on g2408. |
| sensor.py | `STREAM_STATUS` | `sensor.dreame_a2_mower_stream_status` | `property_key=DreameMowerProperty.STREAM_STATUS` → s10001p1; `exists_fn` checks `capability.camera_streaming or` default exists_fn; `capability.camera_streaming = False` (types.py:1196); default `exists_fn` returns True only if the property is non-None in device data | none (s10001p1 = STREAM_STATUS listed as "never observed" in capability snapshot — s10001.* entire service is camera-streaming, not on g2408 lawn mower; `capability.camera_streaming = False`) | DELETE | — | — | — | `capability.camera_streaming = False` on g2408 (types.py:1196). `exists_fn` at sensor.py:293–295 requires `camera_streaming` OR a non-None device data value — the latter will also be None since s10001p1 is never pushed. s10001.* is the vacuum's camera-streaming service, absent on the g2408 mower. Dead code. |
| sensor.py | `CHARGING_STATUS` | `sensor.dreame_a2_mower_charging_status` | `property_key=DreameMowerProperty.CHARGING_STATUS` → s3p2; `entity_category=EntityCategory.DIAGNOSTIC` | s3.2 (confirmed — §2.1 table: "3.2 CHARGING_STATUS, int {0,1,2}, 0=not charging on g2408") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | s3p2 is confirmed on g2408 (§2.1). Redundant with `sensor.state` during normal charging (both say "charging") but can diverge (sensor.py:319–322 comment: "e.g. after user-cancel on lawn state=idle while charging_status=not charging"). Kept under Diagnostic per existing entity_category. Confirms the sensor.py comment rationale for keeping it. |
| sensor.py | `slam_activity` | `sensor.dreame_a2_mower_slam_activity` | `value_fn` → `device.slam_activity` → `device._s2p65_last` (s2p65 SLAM task-type label string) | s2.65 (confirmed — §2.1 table: "2.65 SLAM task-type string, confirmed, string label") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | s2p65 is confirmed on g2408. Surfaces the most-recent SLAM task-type label (e.g. `TASK_SLAM_RELOCATE` during LiDAR relocalization). Diagnostic: user sees at a glance when the mower is relocating (useful during "Positioning Failed" troubleshooting). Latched — does not auto-clear when SLAM task ends (sensor.py:327: "treat a stale value as 'last known'"). Already `entity_category=DIAGNOSTIC` (sensor.py:334). |
| sensor.py | `BATTERY_LEVEL` | `sensor.dreame_a2_mower_battery_level` | `property_key=DreameMowerProperty.BATTERY_LEVEL` → s3p1; `device_class=BATTERY`, `state_class=MEASUREMENT` | s3.1 (confirmed — §2.1 table: "3.1 BATTERY_LEVEL, 0..100%") | PRODUCTION | — | `None` | `True` | Primary battery sensor. s3p1 is confirmed on g2408 with `device_class=BATTERY` for HA standard battery integration. |
| sensor.py | `BLADES_LEFT` | `sensor.dreame_a2_mower_blades_left` | `property_key=DreameMowerProperty.BLADES_LEFT` → s9p2; `entity_category=DIAGNOSTIC` | none (s9p2 not in §2.1 catalog; no observation of s9 properties in any 3-week MQTT capture; CFG.CMS is the confirmed g2408 consumable-wear source via blade_health_pct sensor below) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | s9p2 not confirmed on g2408 — the confirmed consumable data is in CFG.CMS (sensor.py:1398: `blade_health_pct` from `_wear_health(device.cfg.get("CMS"), 0, 6000)`). s9p2 is a vacuum-era consumable register that maps to a different service. Entity likely shows as Unavailable on g2408. Disable by default; DELETE if still Unavailable post-release. The `blade_health_pct` sensor from CFG.CMS is the correct replacement. |
| sensor.py | `BLADES_TIME_LEFT` | `sensor.dreame_a2_mower_blades_time_left` | `property_key=DreameMowerProperty.BLADES_TIME_LEFT` → s9p1; `entity_category=DIAGNOSTIC` | none (s9p1 not in §2.1 catalog; same reasoning as BLADES_LEFT — no s9 observation on g2408) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Same reasoning as `BLADES_LEFT` — s9p1 not observed on g2408. CFG.CMS is the confirmed wear source. Disable by default. |
| sensor.py | `SIDE_BRUSH_LEFT` | `sensor.dreame_a2_mower_side_brush_left` | `property_key=DreameMowerProperty.SIDE_BRUSH_LEFT` → s10p2; `entity_category=DIAGNOSTIC` | none (s10p2 not in §2.1 catalog; no s10 observation in 3-week MQTT capture; CFG.CMS[1] via brush_health_pct is the confirmed side-brush wear source) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | s10p2 not confirmed on g2408. CFG.CMS[1] (`brush_health_pct`) is the confirmed replacement. Disable by default. |
| sensor.py | `SIDE_BRUSH_TIME_LEFT` | `sensor.dreame_a2_mower_side_brush_time_left` | `property_key=DreameMowerProperty.SIDE_BRUSH_TIME_LEFT` → s10p1; `entity_category=DIAGNOSTIC` | none (s10p1 not in §2.1 catalog; no s10 observation on g2408) | EXPERIMENTAL | — | `EntityCategory.DIAGNOSTIC` | `False` | Same reasoning as `SIDE_BRUSH_LEFT` — s10p1 not observed on g2408. Disable by default. |
| sensor.py | `FILTER_LEFT` | `sensor.dreame_a2_mower_filter_left` | `property_key=DreameMowerProperty.FILTER_LEFT` → s11p1; `entity_category=DIAGNOSTIC` | none (s11p1 not in §2.1 catalog; no s11 observation in 3-week MQTT capture; g2408 has no replaceable filter; task spec §"vacuum-only consumables" lists filter as DELETE candidate) | DELETE | — | — | — | s11p1 is a vacuum-era filter register and the g2408 mower has no replaceable filter. No §2.1 entry. No MQTT observation. Unlike BLADES/SIDE_BRUSH which could theoretically apply to a mower, a filter consumable is pure vacuum hardware. Sensor shows permanently Unavailable on g2408. |
| sensor.py | `FILTER_TIME_LEFT` | `sensor.dreame_a2_mower_filter_time_left` | `property_key=DreameMowerProperty.FILTER_TIME_LEFT` → s11p2; `entity_category=DIAGNOSTIC` | none (s11p2 not in §2.1 catalog; no s11 observation; no filter on g2408) | DELETE | — | — | — | Same reasoning as `FILTER_LEFT` — s11p2 is a vacuum-era filter consumable, not applicable to the g2408 mower. Permanently Unavailable. Delete alongside FILTER_LEFT. |
| sensor.py | `FIRST_CLEANING_DATE` | `sensor.dreame_a2_mower_first_cleaning_date` | `property_key=DreameMowerProperty.FIRST_CLEANING_DATE` → s12p1; `entity_category=DIAGNOSTIC`; `device_class=TIMESTAMP` | CFG (confirmed — protocol doc §6 CFG catalog: "FIRST_CLEANING_DATE → sensor.first_mowing_date"; sensor.py:400 comment: "re-added in alpha.102 after alpha.78-81 cloud-URL breakthrough unblocked a whole category of previously-failing RPCs") | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | siid=12 cleaning-history service. Confirmed working post-alpha.102 via the cloud URL breakthrough. Displays first-ever mow timestamp — useful for warranty tracking / device-age awareness. `device_class=TIMESTAMP` correct. |
| sensor.py | `TOTAL_CLEANING_TIME` | `sensor.dreame_a2_mower_total_cleaning_time` | `property_key=DreameMowerProperty.TOTAL_CLEANING_TIME` → s12p2; `entity_category=DIAGNOSTIC`; `device_class=DURATION`; `state_class=TOTAL_INCREASING` | CFG (confirmed — same siid=12 service as FIRST_CLEANING_DATE; cumulative total mowing time in minutes) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | siid=12 lifetime-total confirmed post-alpha.102. Cumulative total mowing minutes across all sessions — useful for fleet health monitoring and consumable-life calibration. |
| sensor.py | `CLEANING_COUNT` | `sensor.dreame_a2_mower_cleaning_count` | `property_key=DreameMowerProperty.CLEANING_COUNT` → s12p3; `state_class=TOTAL_INCREASING`; `entity_category=DIAGNOSTIC` | CFG (confirmed — siid=12 service; cumulative session count) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | siid=12 lifetime session counter confirmed post-alpha.102. Total sessions across device lifetime — useful for wear-rate estimation. |
| sensor.py | `TOTAL_CLEANED_AREA` | `sensor.dreame_a2_mower_total_cleaned_area` | `property_key=DreameMowerProperty.TOTAL_CLEANED_AREA` → s12p4; `state_class=TOTAL_INCREASING`; `entity_category=DIAGNOSTIC` | CFG (confirmed — siid=12 service; cumulative total area in m²) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | siid=12 cumulative mowed area confirmed post-alpha.102. Running total of all mowed area across device lifetime — useful for consumable replacement scheduling. |
| sensor.py | `cruising_history` | `sensor.dreame_a2_mower_cruising_history` | `value_fn` → `device.status.last_cruising_time`; `exists_fn` checks `capability.map and capability.cruising` | none (`capability.cruising` is True only when s8p5 CRUISE_SCHEDULE is non-None — types.py:1240; CRUISE_SCHEDULE listed as "never observed" in capability snapshot line 37; entity never registers on g2408) | DELETE | — | — | — | `capability.cruising = False` on g2408 because s8p5 CRUISE_SCHEDULE is never observed. `exists_fn` at sensor.py:444 blocks registration. Cruising (patrol-mode scheduling) is a vacuum/camera feature not present on the g2408 mower. Dead code on g2408. |
| sensor.py | `CLEANING_PROGRESS` | `sensor.dreame_a2_mower_cleaning_progress` | `property_key=DreameMowerProperty.CLEANING_PROGRESS` → s4p63 (vacuum "cleaning-progress %" register, siid=4 piid=63 per types.py:708) | none (s4p63 not in §2.1 catalog; not in observed-property list; progress data on g2408 comes from session-summary area_mowed/total_lawn ratio in the OSS JSON) | EXPERIMENTAL | — | `None` | `False` | s4p63 not confirmed on g2408 — no MQTT observation. `entity_category=None` at sensor.py:453 (would appear in main entity list when instantiated). Disable by default since it will show Unavailable on g2408; EXPERIMENTAL because it might work on other Dreame mower firmware variants. |
| sensor.py | `firmware_version` | `sensor.dreame_a2_mower_firmware_version` | `value_fn` → `device.info.version`; `entity_category=DIAGNOSTIC` | none (HA-only: populated from the device info object during config-entry setup; not a MQTT property) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | Device firmware version string from `device.info.version`. HA-internal — no §2.1 citation needed. User-actionable for update tracking. |
| sensor.py | `mowing_position_x` (key) | `sensor.dreame_a2_mower_mowing_position_x` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `round(value.x_m, 2)`; `exists_fn=True` always | s1.4 (confirmed — §2.1 MOWING_TELEMETRY, 33-byte position+phase frame; x_m decoded from bytes [1-2]) | PRODUCTION | — | `None` | `True` | Live mower X position in metres from dock. Decoded from confirmed s1.4 telemetry. Feeds dashboard map overlay and automations. `exists_fn=True` required because s1p4 is only emitted during active mowing — entity must register at setup time (sensor.py:463–464 comment). |
| sensor.py | `mowing_position_y` (key) | `sensor.dreame_a2_mower_mowing_position_y` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `round(value.y_m, 2)` (includes ×0.625 calibration factor); `exists_fn=True` always | s1.4 (confirmed — §2.1 MOWING_TELEMETRY; y_m decoded from bytes [3-4] with ×0.625 calibration factor) | PRODUCTION | — | `None` | `True` | Live mower Y position in metres from dock. The ×0.625 calibration factor is confirmed at sensor.py:477–481 (two independent data points: raw 15855 ≈ 10 m tape-measured, raw 16624 = 10.3 m). Confirmed on g2408. |
| sensor.py | `mowing_trace_index` (key) | `sensor.dreame_a2_mower_mowing_trace_index` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `value.trace_start_index`; `entity_category=DIAGNOSTIC`; `entity_registry_enabled_default=False` | s1.4 (confirmed — bytes [7-9] of the 33-byte telemetry frame carry the monotonically-increasing path-point sequence counter, per protocol doc §4.1 s1p4 decode table) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `False` | Path-point sequence counter from s1p4 bytes [7-9]. Protocol-debugging surface: useful for detecting dropped telemetry frames and cross-referencing cloud path data by index. Already disabled by default (sensor.py:509). Stays OBSERVABILITY — no user-actionable use case, purely for protocol RE. |
| sensor.py | `mowing_position_north_m` (key) | `sensor.dreame_a2_mower_mowing_position_north_m` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `_project_to_compass(value, device, "north")` (compass projection using `station_bearing_deg`); `exists_fn=True` always | s1.4 (confirmed — same s1p4 source as x/y; North is a computed projection; `station_bearing_deg` from HA config entry option) | PRODUCTION | — | `None` | `True` | Compass-north displacement in metres, computed from confirmed s1.4 x/y telemetry + user-configured dock bearing (`number.station_direction`). Semantically meaningful even at default bearing=0 (North=X, East=Y). Primary dashboard position display for users who have calibrated dock direction. |
| sensor.py | `mowing_position_east_m` (key) | `sensor.dreame_a2_mower_mowing_position_east_m` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `_project_to_compass(value, device, "east")`; `exists_fn=True` always | s1.4 (confirmed — same as North sensor) | PRODUCTION | — | `None` | `True` | Compass-east displacement in metres. Same projection as North sensor but on the perpendicular axis. Pair of North+East gives a compass-frame 2D position suitable for external map plotting or automation triggers ("mower is in the north-east corner of the lawn"). |
| sensor.py | `mowing_phase` (key) | `sensor.dreame_a2_mower_mowing_phase` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `_mowing_zone_display(value, device)` (maps byte[8] zone-ID to segment name or 1-based index); entity name = "Mowing Zone"; `exists_fn=True` always | s1.4 (confirmed — phase/zone byte at offset [8] of the 33-byte telemetry frame; §2.1 s1p4 row; zone-name resolution via `device.status.segments` from cloud map) | PRODUCTION | — | `None` | `True` | Current mowing zone name/number decoded from confirmed s1.4 telemetry byte[8]. Maps 0-indexed firmware zone-ID to user-readable zone name (from cloud map segments). Primary "which zone is the mower in right now?" indicator. Key stays `mowing_phase` for backward-compat (sensor.py:541). |
| sensor.py | `session_area_mowed` (key) | `sensor.dreame_a2_mower_session_area_mowed` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `value.area_mowed_m2`; `exists_fn=True` always | s1.4 (confirmed — §2.1: "area_mowed_m2 decoded from s1.4 bytes [10-11]") | PRODUCTION | — | `None` | `True` | Current-session area mowed in m², decoded from confirmed s1.4 telemetry. Live progress indicator. Resets on new session. Primary user-facing progress metric during mowing. |
| sensor.py | `session_distance` (key) | `sensor.dreame_a2_mower_session_distance` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `value.distance_m`; `exists_fn=True` always | s1.4 (confirmed — §2.1: distance_m decoded from s1.4 bytes [12-15]; confirmed in protocol doc §4.1 table with sample values) | PRODUCTION | — | `None` | `True` | Current-session distance travelled in metres, decoded from confirmed s1.4 telemetry. Complements `session_area_mowed` for coverage-pattern analysis. |
| sensor.py | `heading_deg` (key) | `sensor.dreame_a2_mower_heading_deg` | `property_key=DreameMowerProperty.MOWING_TELEMETRY` → s1p4; `value_fn` → `round(value.heading_deg, 1)` (0..360°, dock-relative frame); `exists_fn=True` always | s1.4 (confirmed — heading_deg from byte[6] of 33-byte frame confirmed 2026-04-24 via motion-direction correlation across 5586 samples with median error 13°, per sensor.py:578–581) | PRODUCTION | — | `None` | `True` | Mower heading in degrees (dock-relative: 0° = dock-facing direction). Confirmed on g2408 with empirical validation across 5586 telemetry samples (sensor.py:578–581). Useful for tracking mowing direction/pattern and detecting stuck/spinning behaviour. |
| sensor.py | `edgemaster` (key) | `sensor.dreame_a2_mower_edgemaster` | `value_fn` reads `device.get_property(DreameMowerProperty.FRAME_INFO)[2]` → s6p2[2] (bool); `exists_fn=True` always | s6.2 (confirmed — §2.1: "6.2 FRAME_INFO [mowing_height_mm, mow_mode, edgemaster, ?] (settings-saved tripwire)"; confirmed 2026-04-26 by toggle: True→False→True→False) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | EdgeMaster setting state from s6p2[2], confirmed on g2408 2026-04-26 (sensor.py:593–595 comment: "True→False→True→False with no other change"). Boolean sensor surfacing the active EdgeMaster mode — directly user-actionable: edge-mowing effectiveness depends on this being enabled. |
| sensor.py | `mowing_height` (key) | `sensor.dreame_a2_mower_mowing_height` | `value_fn` reads `device.get_property(DreameMowerProperty.FRAME_INFO)[0] / 10.0` → s6p2[0] / 10 → cm; `exists_fn=True` always | s6.2 (confirmed — §2.1 FRAME_INFO; s6p2[0] = mowing height in mm; confirmed 2026-04-26: "always was the height, earlier 'profile_id' hypothesis was wrong", sensor.py:607–610) | PRODUCTION | — | `None` | `True` | Current mowing height in cm, decoded from confirmed s6p2[0]. Range 30–70 mm = 3.0–7.0 cm in 5 mm steps. Primary user-facing cut-height indicator. Confirmed on g2408. |
| sensor.py | `mow_mode` (key) | `sensor.dreame_a2_mower_mow_mode` | `value_fn` → `{0: "standard", 1: "efficient"}.get(device.get_property(DreameMowerProperty.FRAME_INFO)[1])`; `exists_fn=True` always | s6.2 (confirmed — §2.1 FRAME_INFO; s6p2[1] = mow_mode 0=Standard, 1=Efficient; confirmed 2026-04-26 by toggle, sensor.py:631–637) | PRODUCTION | — | `None` | `True` | Current mow mode (Standard / Efficient), decoded from confirmed s6p2[1]. Confirmed on g2408 2026-04-26. Note: the switch.mow_mode_efficient entity exists but is disabled (exists_fn=False); this sensor is the authoritative read-only view of the mow-mode state. |
| sensor.py | `headlight_enabled` (key) | `sensor.dreame_a2_mower_headlight_enabled` | `value_fn` reads `device.cfg["LIT"][0]` → CFG.LIT[0] (bool, custom LED period on/off); `available_fn=_cfg_key_present("LIT")` | CFG.LIT (confirmed — §2.1 CFG catalog: "LIT[0]: Custom LED Activation Period on/off, confirmed 2026-04-24"; protocol doc line 1134) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.LIT[0] confirmed on g2408. On/off state of the custom LED activation period (whether headlights can activate). User-facing: the primary toggle for "does the mower have its lights on schedule?". CONFIG category because it's a settings readout. |
| sensor.py | `headlight_schedule` (key) | `sensor.dreame_a2_mower_headlight_schedule` | `value_fn` → `_format_time_window(device.cfg.get("LIT"))` → "HH:MM-HH:MM" string; full per-field attributes; `available_fn=_cfg_key_present("LIT", min_len=3)` | CFG.LIT (confirmed — §2.1 CFG catalog; LIT shape confirmed: `[enabled, start_min, end_min, standby, working, charging, error, unknown]`) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.LIT LED schedule window. Displays "21:00-07:00" style time window from confirmed CFG.LIT[1-2]. Per-scenario attributes (standby/working/charging/error flags) provide fine-grained LED settings breakdown. CONFIG category. |
| sensor.py | `auto_recharge_standby` (key) | `sensor.dreame_a2_mower_auto_recharge_standby` | `value_fn` reads `device.cfg.get("STUN")` → CFG.STUN {0: off, 1: on}; `available_fn=_cfg_key_present("STUN")` | CFG.STUN (confirmed — §2.1 CFG catalog: "Auto Recharge After Extended Standby, confirmed 2026-04-24"; sensor.py:713–716 comment) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.STUN confirmed on g2408. On/off for auto-recharge when battery drains during standby. Previously mislabelled as "anti_theft" in upstream vacuum codebase (sensor.py:714 comment). User-facing setting. |
| sensor.py | `anti_theft` (key) | `sensor.dreame_a2_mower_anti_theft` | `value_fn` reads `device.cfg["ATA"][:3]` — any() → on/off; per-flag (lift_alarm, offmap_alarm, realtime_location) in attrs; `available_fn=_cfg_key_present("ATA")` | CFG.ATA (confirmed — §2.1 CFG catalog: "Anti-Theft Alarm, confirmed 2026-04-24, shape [lift_alarm, offmap_alarm, realtime_location]"; also feeds device_tracker.gps entity via ATA[2]) | PRODUCTION | — | `None` | `True` | CFG.ATA confirmed on g2408. Three-flag anti-theft alarm aggregate sensor. "on" = any sub-flag enabled. Per-flag attribute breakdown in attributes. User-actionable: enables/disables theft alerts. GPS tracker entity (section 1.1) uses ATA[2] (realtime_location) to determine if GPS is active. |
| sensor.py | `weather_reference` (key) | `sensor.dreame_a2_mower_weather_reference` | `value_fn` reads `device.cfg.get("WRF")` → truthy/falsy; `available_fn=_cfg_key_present("WRF")` | CFG.WRF (confirmed — in §2.1 CFG catalog: "Weather Reference / auto-pause on rain forecast"; sensor.py:757 comment) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.WRF confirmed on g2408. Weather-reference setting (whether the mower uses cloud weather forecast to automatically pause for rain). CONFIG category. Distinct from `binary_sensor.rain_protection_active` (which is the current-state signal) and `sensor.rain_protection` (CFG.WRP — the rain protection settings). |
| sensor.py | `navigation_path` (key) | `sensor.dreame_a2_mower_navigation_path` | `value_fn` → `{0: "direct", 1: "smart"}.get(device.cfg.get("PROT"))`; `available_fn=_cfg_key_present("PROT")` | CFG.PROT (confirmed — §2.1 CFG catalog: "Navigation Path {0: direct, 1: smart}, confirmed 2026-04-24 via isolated single-toggle: nav smart→direct flipped PROT 1→0 with no other CFG key moving", sensor.py:767–772) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.PROT confirmed on g2408 via isolated toggle test. Navigation path strategy setting. CONFIG category. User-actionable: Direct = straight-line approach to each strip; Smart = optimised path avoiding obstacles before the mow strip. |
| sensor.py | `cfg_path_raw` (key) | `sensor.dreame_a2_mower_cfg_path_raw` | `value_fn` → `device.cfg.get("PATH")`; `entity_category=DIAGNOSTIC`; `entity_registry_enabled_default=False`; `available_fn=_cfg_key_present("PATH")` | CFG.PATH (observed but semantics TBD — sensor.py:786–798 comment: "observed stable at 1 through the Navigation Path toggle test, so NOT the Navigation Path setting") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `False` | CFG.PATH is a confirmed CFG key on g2408 (present in every getCFG response) but its semantic meaning is unknown — it did not flip during the CFG.PROT toggle test (sensor.py:787–789). Surfaced as a disabled diagnostic to enable future toggle-correlation research. Correctly disabled by default (sensor.py:794). |
| sensor.py | `child_lock_cfg` (key) | `sensor.dreame_a2_mower_child_lock_cfg` | `value_fn` reads `device.cfg.get("CLS")` → {0: off, 1: on}; `available_fn=_cfg_key_present("CLS")` | CFG.CLS (confirmed — §2.1 CFG catalog: "Child Lock, confirmed 2026-04-24, {0: off, 1: on}") | PRODUCTION | — | `None` | `True` | CFG.CLS confirmed on g2408 as the authoritative child-lock state read-path (sensor.py:799–804 comment: "authoritative read path is CFG.CLS ... provides visible state regardless of whether the switch's s2p backing property is actually emitted"). Companion to `switch.child_lock`. |
| sensor.py | `ai_obstacle_photos` (key) | `sensor.dreame_a2_mower_ai_obstacle_photos` | `value_fn` reads `device.cfg.get("AOP")` → {0: off, 1: on}; `available_fn=_cfg_key_present("AOP")` | CFG.AOP (confirmed — §2.1 CFG catalog: "Capture Photos of AI-Detected Obstacles, confirmed 2026-04-24, {0: off, 1: on}"; sensor.py:815–817) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.AOP confirmed on g2408. Privacy-relevant setting: whether the mower captures photos of detected obstacles and uploads them. User-actionable consent setting. CONFIG category. |
| sensor.py | `frost_protection` (key) | `sensor.dreame_a2_mower_frost_protection` | `value_fn` → `{0: "off", 1: "on"}.get(device.cfg.get("FDP"))`; `available_fn=_cfg_key_present("FDP")` | CFG.FDP (confirmed — §2.1 CFG catalog: "Frost Protection, confirmed 2026-04-24 via isolated single-toggle", sensor.py:827–830) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.FDP confirmed on g2408 via isolated toggle test (sensor.py:827–830). Frost protection prevents mowing when temperature is too low. User-actionable seasonal setting. CONFIG category. |
| sensor.py | `human_presence_alert` (key) | `sensor.dreame_a2_mower_human_presence_alert` | `value_fn` reads `device.cfg["REC"][0]` → on/off; 9-element attributes (sensitivity, scenario flags, photo consent, push interval); `available_fn=_cfg_key_present("REC", min_len=9)` | CFG.REC (confirmed — §2.1 CFG catalog: "Human Presence Detection Alert, confirmed 2026-04-24, 9-element shape"; matches s2p51 HUMAN_PRESENCE_ALERT decoder) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.REC confirmed on g2408 with full 9-element decode. Human-presence detection alert settings — user-actionable safety/privacy setting with per-sensitivity and per-scenario granularity in attributes. Note: the AI human-detection feature on g2408 uses CFG.REC / s2p51 transport, NOT the s4p22 AI_DETECTION property (which was deleted from switch.py as dead code). |
| sensor.py | `dnd_schedule` (key) | `sensor.dreame_a2_mower_dnd_schedule` | `value_fn` reads `device.cfg["DND"]` → time-window string or "off"; attributes: enabled, start_min, end_min; `available_fn=_cfg_key_present("DND", min_len=3)` | CFG.DND (confirmed — §2.1 CFG catalog: "Do Not Disturb, confirmed 2026-04-24, shape [enabled, start_min, end_min]"; sensor.py:888–893 comment confirms element order differs from s2p51 DND event payload) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.DND confirmed on g2408. DND schedule sensor shows "21:00-07:00" when enabled or "off" when disabled. Note: the `time.dnd_start/end` entities (section 1.2) were DELETE because they gate on `capability.dnd = False` — but this sensor reads CFG.DND directly and bypasses that gate, so it works on g2408. These are complementary. |
| sensor.py | `low_speed_nighttime` (key) | `sensor.dreame_a2_mower_low_speed_nighttime` | `value_fn` reads `device.cfg["LOW"]` → time-window string or "off"; attributes: enabled, start_min, end_min; `available_fn=_cfg_key_present("LOW", min_len=3)` | CFG.LOW (confirmed — §2.1 CFG catalog: "Low-Speed Nighttime, confirmed 2026-04-24, shape [enabled, start_min, end_min]"; matches s2p51 LOW_SPEED_NIGHT decoder, sensor.py:926–929) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.LOW confirmed on g2408. Shows the low-speed nighttime window ("20:00-08:00") or "off". Reduces mowing noise during night hours. User-actionable neighbour-considerate setting. CONFIG category. |
| sensor.py | `rain_protection` (key) | `sensor.dreame_a2_mower_rain_protection` | `value_fn` reads `device.cfg["WRP"][0]` → on/off; attributes: enabled, resume_hours, auto_resume; `available_fn=_cfg_key_present("WRP", min_len=2)` | CFG.WRP (confirmed — §2.1 CFG catalog: "Rain Protection, confirmed 2026-04-24, shape [enabled, resume_hours]"; matches s2p51 RAIN_PROTECTION decoder, sensor.py:960–965) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.WRP confirmed on g2408. Rain protection setting: whether to pause mowing during rain and how many hours to wait before auto-resuming. Distinct from `binary_sensor.rain_protection_active` (current rain-pause state) and `sensor.weather_reference` (forecast-based pause). CONFIG category. |
| sensor.py | `charging_config` (key) | `sensor.dreame_a2_mower_charging_config` | `value_fn` reads `device.cfg["BAT"]` → custom-charging window "HH:MM-HH:MM" or "off"; attributes: recharge_pct, resume_pct, unknown_flag, custom_charging_enabled, start_min, end_min; `available_fn=_cfg_key_present("BAT", min_len=6)` | CFG.BAT (confirmed — §2.1 CFG catalog: "Charging config, confirmed 2026-04-24, 6-element shape"; sensor.py:998–1007) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.BAT confirmed on g2408 with 6-element decode. Shows the custom charging window and exposes recharge/resume percentages in attributes. richer than the deleted `switch.off_peak_charging` (which was s3p3, never observed). CONFIG category. |
| sensor.py | `robot_voice` (key) | `sensor.dreame_a2_mower_robot_voice` | `value_fn` reads `device.cfg["LANG"][1]` → voice index label ({0: "default", 7: "Norwegian", ...}); attributes: text_idx, voice_idx; `available_fn=_cfg_key_present("LANG", min_len=2)` | CFG.LANG (confirmed — §2.1 CFG catalog: "Language [text_idx, voice_idx], confirmed 2026-04-24 via live Robot Voice toggle; voice_idx=7 = Norwegian"; sensor.py:1041–1045) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.LANG confirmed on g2408. Surfaces active voice language selection. User-actionable: allows verifying which voice pack is currently active without opening the app. CONFIG category. |
| sensor.py | `voice_prompt_modes` (key) | `sensor.dreame_a2_mower_voice_prompt_modes` | `value_fn` → `sum(bool(x) for x in device.cfg["VOICE"][:4])` (0..4 count of enabled modes); attributes: regular_notification, work_status, special_status, error_status; `available_fn=_cfg_key_present("VOICE", min_len=4)` | CFG.VOICE (confirmed — §2.1 CFG catalog: "Voice Prompt Mode toggles, confirmed 2026-04-24, 4-element bool array"; sensor.py:1073–1076) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.VOICE confirmed on g2408. Shows how many voice modes are enabled (0–4) with per-mode breakdown in attributes. User-actionable: controls whether the mower speaks during regular, work-status, special-status, and error conditions. CONFIG category. |
| sensor.py | `robot_voice_volume` (key) | `sensor.dreame_a2_mower_robot_voice_volume` | `value_fn` reads `device.cfg["VOL"]` → int 0–100; `native_unit_of_measurement="%"`; `available_fn=_cfg_key_present("VOL")` | CFG.VOL (confirmed — §2.1 CFG catalog: "Robot Voice volume, 0..100%, confirmed 2026-04-24"; sensor.py:1102–1103) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.VOL confirmed on g2408. Read-only counterpart to the `number.volume` entity (which writes). Shows current configured volume. CONFIG category. |
| sensor.py | `cfg_version` (key) | `sensor.dreame_a2_mower_cfg_version` | `value_fn` reads `device.cfg.get("VER")` → int; `entity_category=DIAGNOSTIC`; `available_fn=_cfg_key_present("VER")` | CFG.VER (confirmed — §2.1 CFG catalog: "CFG-update revision counter, confirmed 2026-04-24, NOT firmware version as previously documented; bumps by 1 on every successful CFG write"; sensor.py:1115–1120) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG.VER is a protocol-debugging surface: the counter increments on every confirmed CFG write, making it a reliable tripwire for toggle-correlation research ("did my CFG write land?"). Not user-actionable but useful to keep enabled under Diagnostic for all users — helps diagnose CFG write failures. OBSERVABILITY not PRODUCTION because it has no direct user-facing meaning. |
| sensor.py | `obs_keys_raw` (key) | `sensor.dreame_a2_mower_obs_keys_raw` | `value_fn` → `len(device._obs)` (count of OBS keys) or last error; attributes: full `_obs` dict + fetch metadata; `entity_category=DIAGNOSTIC` | none (HA-internal: OBS is fetched via `device.refresh_obs()` → `getOBS` routed-action; OBS endpoint is a protocol-RE surface, not a confirmed g2408 property channel) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Raw dump of the getOBS (obstacle-avoidance settings) routed-action response. Protocol-RE surface for understanding which obstacle-avoidance parameters the g2408 firmware exposes. Currently enabled by default for research (sensor.py:1143–1145 comment: "research-active territory"). Correct classification: OBSERVABILITY. Consider disabling by default post-RE phase. |
| sensor.py | `aiobs_keys_raw` (key) | `sensor.dreame_a2_mower_aiobs_keys_raw` | `value_fn` → `len(device._aiobs)` or last error; attributes: full `_aiobs` dict + fetch metadata; `entity_category=DIAGNOSTIC` | none (HA-internal: fetched via `device.refresh_aiobs()` → `getAIOBS` routed-action; AI obstacle settings endpoint) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Raw dump of the getAIOBS (AI obstacle settings) routed-action response. Protocol-RE surface for AI obstacle recognition parameters (human/animal/object detection sensitivities, photo consent settings). Research-active; consider disabling by default post-RE phase. |
| sensor.py | `designated_ignore_zones` (key) | `sensor.dreame_a2_mower_designated_ignore_zones` | `value_fn` → `_count_map_zones(device, "notObsAreas")`; attributes: `zones` list; `exists_fn=True` always | s6.1 (confirmed MAP_DATA; cloud map payload key "notObsAreas" confirmed present 2026-04-27: sensor.py:1183–1186 comment "confirmed 2026-04-27 once cloud sync caught up") | PRODUCTION | — | `None` | `True` | Count of Designated Ignore Obstacle zones from cloud MAP.notObsAreas (green zones in app, confirmed on g2408 2026-04-27). Attributes carry full zone geometry (id, type, shapeType, corner-count, path). User-facing: shows which areas the mower ignores obstacle detection. |
| sensor.py | `exclusion_zones` (key) | `sensor.dreame_a2_mower_exclusion_zones` | `value_fn` → `_count_map_zones(device, "forbiddenAreas")`; attributes: `zones` list; `exists_fn=True` always | s6.1 (confirmed MAP_DATA; cloud map payload key "forbiddenAreas" — classic exclusion / no-go zones, red in app; confirmed in protocol doc) | PRODUCTION | — | `None` | `True` | Count of exclusion (no-go) zones from cloud MAP.forbiddenAreas. Attributes carry full zone geometry. User-facing: shows the current exclusion zone configuration. `forbiddenAreas` vs `notObsAreas` discrimination confirmed 2026-04-27 (sensor.py:1186–1191). |
| sensor.py | `spot_zones` (key) | `sensor.dreame_a2_mower_spot_zones` | `value_fn` → `_count_map_zones(device, "spotAreas")`; attributes: `zones` list; `exists_fn=True` always | s6.1 (confirmed MAP_DATA; "spotAreas" confirmed 2026-04-27: type=3 WorkingMode.SPOT, shapeType=7 axis-aligned rectangle, sensor.py:1208–1212) | PRODUCTION | — | `None` | `True` | Count of spot zones from cloud MAP.spotAreas. Confirmed on g2408 2026-04-27. Attributes carry full zone geometry. User-facing: shows configured spot-mow areas. Populated after at least one spot mow has run (cloud sync may lag by hours). |
| sensor.py | `map_keys_raw` (key) | `sensor.dreame_a2_mower_map_keys_raw` | `value_fn` → `len(device._latest_cloud_map_payload)`; attributes: all 17 cloud-map top-level keys (truncated at 800 chars each via `_truncate_map_attrs_fn`); `entity_category=DIAGNOSTIC` | s6.1 (confirmed MAP_DATA; cloud-map payload is fetched in response to s6.1 trigger; all 17 keys are from the confirmed cloud map API) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Raw dump of all cloud-map payload keys for toggle-correlation research. Protocol-RE surface: enables spotting which MAP keys change when settings (zones, obstacles, mowing areas) change. The `_recent_changes` and `_last_diff` sub-attrs in `_truncate_map_attrs_fn` make it easy to see what changed on each cloud sync. Consider disabling by default post-RE phase. |
| sensor.py | `mower_status_raw` (key) | `sensor.dreame_a2_mower_mower_status_raw` | `value_fn` → `int(device.status.status)` → raw DreameMowerStatus enum int; attributes: `status_name`; `entity_category=DIAGNOSTIC` | s2.1 (confirmed — `device.status.status` reads from s2p1 STATE; sensor.py:1239–1248 comment: "never the underlying integer, which makes it impossible to identify new status codes") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Raw s2p1 STATE integer + enum name. Protocol-debugging surface: exposes raw firmware status codes (e.g. code 11 = BUILDING, code 16 = STATION_RESET) that the lawn_mower platform entity doesn't surface. Explicitly added during cleanup (sensor.py:1240 comment: "This sensor surfaces the raw int as state … so we can correlate observed app behaviour with firmware codes"). Stays OBSERVABILITY — useful for protocol RE, not user-facing. |
| sensor.py | `gps_position` (key) | `sensor.dreame_a2_mower_gps_position` | `value_fn` → `f"{device.gps_latitude:.6f},{device.gps_longitude:.6f}"` (from `_locn` cache via `getCFG t:'LOCN'`); `entity_category=DIAGNOSTIC` | LOCN (confirmed — §2.1 CFG catalog: "dock GPS pos, {pos:[lon,lat]}"; same source as device_tracker.gps entity) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | GPS "lat,lon" string diagnostic surface. The device_tracker entity (section 1.1) provides the map-integrated GPS view; this sensor is the raw-value debugging surface (shows whether LOCN fetch succeeded, when it was last fetched, raw _locn dict). Diagnostic OBSERVABILITY appropriate — user uses the device_tracker for navigation, this for debugging. |
| sensor.py | `routed_endpoints_probe` (key) | `sensor.dreame_a2_mower_routed_endpoints_probe` | `value_fn` → count of non-error responses in `device._routed_endpoint_probe`; attributes: per-endpoint results; `entity_category=DIAGNOSTIC` | none (HA-internal: one-shot probe of all apk-listed routed-action GET endpoints at startup; no MQTT property) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Startup probe result showing which apk-listed routed-action endpoints the g2408 actually responds to. Protocol-RE tool: lets researchers see at a glance which endpoints are live without wiring each to a dedicated entity. Sensor.py:1234–1238 comment explains purpose. |
| sensor.py | `cfg_keys_raw` (key) | `sensor.dreame_a2_mower_cfg_keys_raw` | `value_fn` → `len(device.cfg)` (key count); attributes: full CFG dict + `_recent_changes` + `_last_diff` + `_last_diff_at`; `entity_category=DIAGNOSTIC` | CFG.* (confirmed — getCFG response; all confirmed CFG keys; sensor.py:1301–1323) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Full CFG dump for toggle-correlation research. State = key count so any "CFG refetched" tick is visible in state history. Attributes carry every CFG key + value. The `_recent_changes` / `_last_diff` sub-attrs make it easy to correlate which CFG keys changed on a given toggle. Research-active; the individual CFG-decoded sensors (headlight_enabled, navigation_path, etc.) make this less critical post-RE, but keep enabled during ongoing research. |
| sensor.py | `map_fetch_health` (key) | `sensor.dreame_a2_mower_map_fetch_health` | `value_fn` → "ok" / "error" / "no_data"; attributes: attempts, successes, unchanged, failures, last_md5, last_error, timestamps; `entity_category=DIAGNOSTIC` | s6.1 (confirmed — map-fetch health is the health of the cloud-map-poll loop triggered by confirmed s6.1 push; sensor.py:1333–1336 comment) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Cloud-map fetch health sensor. Makes silent MAP-fetch failures visible — the core bug class this sensor exists to surface (sensor.py:1333: "silent-failure is the whole bug class this sensor exists to surface"). Enabled by default for all users — invisible failures to the map-polling loop would otherwise leave all map-derived sensors (zones, obstacles, maintenance points) stale with no user-visible signal. |
| sensor.py | `cfg_fetch_health` (key) | `sensor.dreame_a2_mower_cfg_fetch_health` | `value_fn` → "disabled" / "backoff" / "ok"; attributes: counters, failure reasons, timestamps, next_retry_in_s; `entity_category=DIAGNOSTIC` | CFG.* (confirmed — CFG-fetch health tracks the routed-action getCFG loop; all CFG-derived sensors depend on it) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG fetch health sensor. Makes silent CFG-fetch failures visible — an earlier design had a hard-disable with no user-visible signal (sensor.py:1329: "a silent hard-disable previously blinded all CFG-derived entities with no user-visible signal"). Enabled by default for all users — a CFG fetch failure silently breaks all CFG-derived sensors (headlight_enabled, navigation_path, child_lock_cfg, wear meters, etc.). |
| sensor.py | `blade_health_pct` (key) | `sensor.dreame_a2_mower_blade_health_pct` | `value_fn` → `_wear_health(device.cfg.get("CMS"), 0, 6000)` → % remaining; `native_unit_of_measurement="%"`; `available_fn=_cfg_key_present("CMS", min_len=1)` | CFG.CMS (confirmed — §2.1 CFG catalog: "CMS=[blade,brush,robot,aux] wear minutes, confirmed on g2408: all three shipped sensors match app exactly 57%/91%/29%", sensor.py:1392–1394) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG.CMS[0] blade wear as %-remaining, confirmed on g2408 with live app value cross-check. Max threshold = 6000 minutes per apk (sensor.py:1395). This is the confirmed authoritative blade-life sensor on g2408 (replaces the unconfirmed BLADES_LEFT/BLADES_TIME_LEFT from s9p2/s9p1). DIAGNOSTIC category for consumable-life display. |
| sensor.py | `brush_health_pct` (key) | `sensor.dreame_a2_mower_brush_health_pct` | `value_fn` → `_wear_health(device.cfg.get("CMS"), 1, 30000)` → % remaining; `available_fn=_cfg_key_present("CMS", min_len=2)` | CFG.CMS (confirmed — same CMS source; CMS[1] = brush wear minutes; confirmed matching app) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG.CMS[1] side-brush wear as %-remaining. Max threshold = 30000 minutes (10× blade; longer-lived brush component). Confirmed on g2408. Authoritative side-brush consumable sensor (replaces unconfirmed SIDE_BRUSH_LEFT from s10p2). |
| sensor.py | `robot_maintenance_health_pct` (key) | `sensor.dreame_a2_mower_robot_maintenance_health_pct` | `value_fn` → `_wear_health(device.cfg.get("CMS"), 2, 3600)` → % remaining; `available_fn=_cfg_key_present("CMS", min_len=3)` | CFG.CMS (confirmed — CMS[2] = robot body/frame maintenance wear minutes; max 3600 minutes per apk; confirmed matching app) | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG.CMS[2] robot maintenance health as %-remaining. Max 3600 minutes (~60 hours). Confirmed on g2408. Third CMS slot tracks general robot maintenance interval. |
| sensor.py | `aux_wear_health_pct` (key) | `sensor.dreame_a2_mower_aux_wear_health_pct` | `value_fn` → `_wear_health(device.cfg.get("CMS"), 3, 6000)` → % remaining; `available_fn=_cfg_key_present("CMS", min_len=4)` | CFG.CMS (partial — CMS[3] is the 4th slot observed on g2408 in alpha.85: value was 0; semantics TBD; max-minute guess = 6000, sensor.py:1421–1422: "semantics TBD") | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | CFG.CMS[3] auxiliary wear slot — semantics unknown. Observed present on g2408 but always 0 in alpha.85 (sensor.py:1421). Surfaced as OBSERVABILITY: the existence of the slot is confirmed but its meaning is not. `_cfg_key_present("CMS", min_len=4)` gate ensures it only appears when CMS actually has 4 elements. |
| sensor.py | `mower_timezone` (key) | `sensor.dreame_a2_mower_mower_timezone` | `value_fn` reads `device.cfg.get("TIME")` → str (e.g. "Europe/Oslo"); `available_fn=_cfg_key_present("TIME")` | CFG.TIME (confirmed — §2.1 CFG catalog: "Timezone, confirmed 2026-04-24"; sample "Europe/Oslo" for Norwegian locale) | PRODUCTION | — | `EntityCategory.CONFIG` | `True` | CFG.TIME confirmed on g2408. Mower's configured timezone string. User-facing: verifies the mower is using the correct local timezone (important for scheduled mowing, DND windows, etc.). CONFIG category. |
| sensor.py | `dock_x_cm` (key) | `sensor.dreame_a2_mower_dock_x_cm` | `value_fn` → `device.dock_pos.get("x")`; `available_fn=_dock_pos_present`; triggered by s1p51 dock-pos-update hint | s1.51 (confirmed — §2.1: "1.51 confirmed, empty dict; dock-pos-update hint"; s1p51 triggers `_side_effect_refresh_dock_pos` → `refresh_dock_pos()` → getDockPos routed-action at device.py:405) | PRODUCTION | — | `None` | `True` | Dock X position in cm from getDockPos routed-action, triggered by confirmed s1p51 hint. Useful for verifying dock position after physical dock moves. Feeds map calibration. Available only when `_dock_pos` dict is non-empty. |
| sensor.py | `dock_y_cm` (key) | `sensor.dreame_a2_mower_dock_y_cm` | `value_fn` → `device.dock_pos.get("y")`; same `available_fn` | s1.51 (confirmed — same getDockPos trigger as dock_x_cm) | PRODUCTION | — | `None` | `True` | Dock Y position in cm. Same source and trigger as `dock_x_cm`. |
| sensor.py | `dock_yaw_deg` (key) | `sensor.dreame_a2_mower_dock_yaw_deg` | `value_fn` → `device.dock_pos.get("yaw", 0) / 10.0` (apk spec: yaw / 10 = degrees); `native_unit_of_measurement="°"`; same `available_fn` | s1.51 (confirmed — same getDockPos trigger; yaw / 10 = degrees per apk spec, sensor.py:1464) | PRODUCTION | — | `None` | `True` | Dock yaw angle in degrees from getDockPos. Dock orientation in the mower's coordinate frame. Useful for validating dock installation direction vs the user-configured station_bearing_deg. |
| sensor.py | `dock_lawn_connected` (key) | `sensor.dreame_a2_mower_dock_lawn_connected` | `value_fn` → "yes" / "no" based on `device.dock_pos.get("connect_status")`; same `available_fn` | s1.51 (confirmed — getDockPos response includes connect_status field indicating whether the dock is communicating with the lawn boundary wire) | PRODUCTION | — | `None` | `True` | Dock-to-lawn connection status from getDockPos. Shows whether the dock's lawn-connection (boundary wire or RF link) is active. User-actionable: a "no" here indicates a dock hardware issue that would affect mowing boundary detection. |
| sensor.py | `maintenance_points_count` (key) | `sensor.dreame_a2_mower_maintenance_points_count` | `value_fn` → `len(device.maintenance_points)` (from cloud MAP cleanPoints); attributes: list of {id, x, y} tuples in metres; `state_class=MEASUREMENT` | s6.1 (confirmed MAP_DATA; `cleanPoints` key in cloud map payload; populated during `_build_map_from_cloud_data()` at device.py:2713–2745) | PRODUCTION | — | `None` | `True` | Count of user-placed maintenance points from the confirmed cloud map payload. Attributes carry full point list (id, x_metres, y_metres) for dashboard rendering and automations. Session 1.1 confirms "Go to Maintenance Point" flow is confirmed on g2408. The alpha.113 removal of single-point convenience sensors (sensor.py:1493–1497 comment) was correct: multi-point setup now works via this sensor's attributes. |
| sensor.py | `voice_download_progress` (key) | `sensor.dreame_a2_mower_voice_download_progress` | `value_fn` → `device.voice_dl_progress`; `available_fn` gates on non-None; `native_unit_of_measurement="%"` | s2.53 (partial — §2.1 table: "2.53 voice-pack progress, partial, 0..100") | OBSERVABILITY | — | `None` | `True` | Voice-pack download progress 0..100%, sourced from partially-confirmed s2p53. Partial §2.1 citation (s2p53 observed but not fully characterised). OBSERVABILITY — user needs to see when a voice pack download is in progress but this is not a regular dashboard item. `available_fn` gates entity on download actually being in progress. |
| sensor.py | `self_check_result` (key) | `sensor.dreame_a2_mower_self_check_result` | `value_fn` → "pass" if result==0 else str(result); `available_fn` gates on non-None `device.self_check_result` dict; sourced from s2p58 | s2.58 (partial — §2.1 table: "2.58 self-check result, partial, observed") | OBSERVABILITY | — | `None` | `True` | Self-check result from partially-confirmed s2p58. Shows "pass" or raw result int. OBSERVABILITY — useful for diagnosing hardware issues but not a regular dashboard metric. `available_fn` ensures entity is only available after a self-check has been run (s2p58 push received). |
| sensor.py | `DreameArchivedSessionsSensor` (standalone class) | `sensor.<device_name>_archived_mowing_sessions` (unique_id: `<mac>_archived_sessions`) | HA-internal: reads `coordinator.session_archive.count` + `archive.list_sessions()` + `archive.latest()`; no MQTT property; registered only when `coordinator.session_archive is not None` | session-summary (confirmed — event_occured siid=4 eiid=1 piid=9 = OSS key; the archive is populated by the coordinator when the OSS JSON is downloaded; §2.1 §"session-summary") | PRODUCTION | — | `EntityCategory.DIAGNOSTIC` | `True` | Primary session-history sensor: count of archived mowing sessions with most-recent session and full N-session list in attributes. The core of the user's mowing history view (protocol doc §7.4 session-summary flow confirmed). Built from confirmed session-summary OSS JSON downloads. Correctly gated on `session_archive is not None` (sensor.py:1560–1561). DIAGNOSTIC category keeps it out of the main entity card without hiding it. |
| sensor.py | `DreameArchivedLidarScansSensor` (standalone class) | `sensor.<device_name>_archived_lidar_scans` (unique_id: `<mac>_archived_lidar`) | HA-internal: reads `coordinator.lidar_archive.count` + `archive.list_scans()` + `archive.latest()`; no MQTT property; registered only when `coordinator.lidar_archive is not None` | 99.20 (confirmed — §2.1: "99.20 LiDAR PCD, confirmed, OSS key"; the lidar archive is populated when the coordinator downloads PCD blobs from the confirmed 99.20 OSS key) | OBSERVABILITY | — | `EntityCategory.DIAGNOSTIC` | `True` | Count of archived LiDAR point-cloud scans. OBSERVABILITY because the LiDAR scan archive is a protocol-RE / power-user feature (the camera.lidar_top_down entity renders the actual point cloud). Attributes carry scan list with MD5 hashes. Correctly gated on `lidar_archive is not None` (sensor.py:1564–1565). |
